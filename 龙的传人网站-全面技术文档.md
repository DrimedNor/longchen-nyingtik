# 龙的传人网站 · 全面技术文档

> 文档版本：v1.1
> 更新时间：2026-09-06（v1.1：管理后台第一阶段优化落地，见 7.1 已知问题表）
> 用途：供Workbuddy全面了解项目架构、接手执行技术优化
> 分工：豆包负责整体架构、产品设计、战略决策；Workbuddy负责具体执行、代码编写、技术优化

---

## 一、项目概述

### 1.1 项目定位
- **网站名称**：龙钦宁提资料库
- **域名**：https://longchen-nyingtik.wiki（阿里云购买，Cloudflare DNS）
- **托管**：Cloudflare Pages
- **用途**：个人学习资料整理，兼顾身边朋友交流分享，不传教、不募捐、不组织宗教活动
- **访问控制**：所有IP第一次访问需输入密码（610），密码主要是形式上的"非公开"声明

### 1.2 核心数据
- 文章数：252篇
- 音频数：74个
- 图片数：36个
- HTML大小：约460KB（Brotli压缩后约90KB）
- AI知识库：11.25MB（knowledge.json，按需加载）
- 文章内容拆分：193篇文章拆分为单独JSON文件（按需加载）

### 1.3 技术栈特点
- **零外部依赖**：不使用React/Vue等框架，纯原生HTML/CSS/JS
- **单文件SPA**：所有CSS/JS内联到index.html，部署简单
- **自研构建器**：Python脚本build_site.py（5671行），将Obsidian Markdown转为静态网站
- **Cloudflare Workers**：两个Worker分别处理统计/密码验证和AI问答代理
- **PWA支持**：Service Worker离线缓存、添加到主屏幕、后台播放

---

## 二、系统架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  前端SPA  │  │ 管理后台  │  │ AI搜索面板│  │  播放器   │  │
│  │ index.html│  │ admin.html│  │ (内嵌)    │  │ (内嵌)    │  │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  │
└───────┼──────────────┼──────────────┼──────────────┼────────┘
        │              │              │              │
        ▼              ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Cloudflare   │ │ 统计Worker    │ │ AI问答Worker  │ │ Cloudflare   │
│ Pages        │ │ (统计+密码)   │ │ (API代理)     │ │ KV存储       │
│ (静态文件)   │ │              │ │              │ │ (数据持久化) │
└──────────────┘ └──────┬───────┘ └──────┬───────┘ └──────────────┘
                         │                  │
                         ▼                  ▼
                  ┌──────────────┐  ┌──────────────┐
                  │  访问统计数据 │  │ 腾讯云TokenHub│
                  │  用户注册数据 │  │  DeepSeek API │
                  └──────────────┘  └──────────────┘
```

---

## 三、目录结构详解

### 3.1 项目根目录
```
龙的传人-website/
├── build_site.py          # 核心构建脚本（5671行，自研零依赖）
├── admin.html             # 管理后台页面（1160行）
├── stats-auth-worker.js   # 统计与密码验证Worker（1343行）
├── ai-ask-worker.js       # AI问答Worker（345行）
├── sw.js                  # Service Worker源文件（67行，构建时复制到content/）
├── manifest.json          # PWA manifest源文件
├── _headers               # Cloudflare Pages响应头配置
├── wrangler.toml          # AI问答Worker配置
├── wrangler-stats.toml    # 统计Worker配置
├── generate_pwa_icons.py  # PWA图标生成脚本
├── content/               # 内容源（Obsidian Markdown）
├── dist/                  # 构建产物（部署目录）
├── assets/                # PWA图标源文件
├── audio/                 # 音频源文件
└── 外包任务书-*.md         # 给其他AI的任务书
```

### 3.2 content/ 内容源目录
```
content/
├── index.md               # 首页内容
├── sw.js                  # Service Worker（构建时复制到dist/）
├── assets/                # PWA图标（构建时复制到dist/assets/）
├── 上师开示/              # 上师开示文章（10大分类）
├── 龙钦宁提传承/          # 传承介绍、上师介绍
├── 音频资源/              # 音频文件、法音介绍
├── 书籍/                  # 书籍介绍、传承著作
├── 关于本站/              # 关于本站、新手指南
└── 日志-不推送/           # 建站日志、照见日志（构建时跳过，不发布）
```

### 3.3 dist/ 构建产物目录
```
dist/
├── index.html             # 单文件SPA（所有CSS/JS内联）
├── knowledge.json         # AI知识库全文分段索引（11.25MB，按需加载）
├── manifest.json          # PWA manifest
├── sw.js                  # Service Worker
├── robots.txt             # 搜索引擎规则
├── _headers               # Cloudflare Pages响应头
├── admin/
│   └── index.html         # 管理后台（admin.html复制重命名）
├── assets/                # PWA图标（6个PNG）
├── audio/                 # 音频文件（74个）
└── pages/                 # 文章内容JSON（193篇，按需加载）
```

---

## 四、核心系统详解

### 4.1 构建系统（build_site.py）

**文件位置**：`build_site.py`（5671行）

**核心功能**：
1. 扫描content/目录下的所有Markdown文件
2. 解析frontmatter（标题、标签、日期等）
3. 将Markdown转为HTML
4. 构建目录树（支持多级嵌套）
5. 生成AI知识库索引（knowledge.json）
6. 拆分文章内容为单独JSON（pages/目录）
7. 生成封面图、复制PWA图标
8. 复制管理后台、Service Worker、音频文件
9. 输出单文件SPA（dist/index.html）

**主要函数**：
| 函数名 | 功能 |
|--------|------|
| `natural_sort_key(s)` | 自然排序（支持中文、数字混合排序） |
| `parse_frontmatter(text)` | 解析Markdown frontmatter |
| `clean_dir_name(name)` | 清理目录名（去掉序号前缀等） |
| `audio_folder_rel(fname)` | 获取音频文件相对路径 |
| `inline(text)` | 行内Markdown转HTML |
| `md_to_html(body)` | Markdown正文转HTML |
| `is_excluded_dir(d)` | 判断目录是否排除（隐藏目录、不推送目录、备份目录、assets目录） |
| `discover_pages()` | 扫描所有页面 |
| `build_tree(pages)` | 构建目录树 |
| `gen_cover_png(path, size)` | 生成封面图 |
| `watch_and_serve()` | 本地开发模式（监听文件变化+本地服务器） |
| `main()` | 主构建函数 |

**构建命令**：
```bash
# 完整构建
python build_site.py

# 本地开发模式（监听变化+本地服务器）
python build_site.py --watch
```

**关键设计决策**：
- 单文件SPA：所有CSS/JS内联到index.html，部署简单，加载快
- 零外部依赖：不使用任何前端框架，纯原生JS，稳定性高
- 文章按需加载：文章内容拆分为单独JSON，首次加载只加载目录，点击文章时才加载内容
- AI知识库按需加载：knowledge.json 11.25MB，首次使用AI搜索时才加载，Service Worker缓存

---

### 4.2 前端SPA系统（index.html）

**文件位置**：`dist/index.html`（构建产物，源在build_site.py中）

**核心模块**：

#### 4.2.1 路由系统
- **Hash路由**：使用URL hash（`#/路径`），支持SPA无刷新跳转
- **统一导航入口**：`go(path)`函数，所有页面跳转都经过此函数
- **返回手势优化**：支持手机端左滑返回，退到首页时不再退出（已删除退出确认守卫）

#### 4.2.2 目录展示系统
- **多级目录树**：支持最多3级目录嵌套
- **默认折叠**：一级目录默认折叠，显示引导语和文章数量
- **工具栏**：每个一级目录有"展开/折叠全部"和"AI搜索"按钮
- **展开全部**：点击"展开全部目录"展开所有层级
- **数量显示**：文章数量紧挨着标题名称

#### 4.2.3 播放器系统
- **全局播放器**：底部悬浮播放器，支持连播
- **专辑功能**：上师开示（AI朗读）、上师法音、上师赞歌三个专辑
- **播放控制**：点击—最小化到页面底部横栏（继续播放），点击✕关闭播放器（同时关闭音频）
- **后台播放**：支持锁屏控制（Media Session API），Chrome浏览器可用
- **音频预加载**：播放到80%时预加载下一首，数据节省模式或慢网络不预加载
- **首次播放提示**：第一次播放时弹出用户选择提示（是否开启连播预加载）

#### 4.2.4 AI搜索系统
- **搜索与AI分离**：默认只显示相关文章列表（速度快、准确），下方有"🤖 用 AI 问答"按钮，用户按需点击
- **AI API端点**：`https://ai.longchen-nyingtik.wiki`（自定义域名，避免workers.dev在国内被DNS污染）
- **超时降级**：15秒超时（AbortController），失败时调用`renderAiFallback()`显示本地搜索结果
- **知识库索引**：全文分段索引（16127条），按需加载knowledge.json
- **引用来源**：AI回答时列出引用的文章，点击可跳转
- **引用过滤**：永远不引用更新日志；上师开示有匹配时不显示三部开示集
- **面板样式**：接近全屏显示（95%宽度，max-width 900px，max-height 92vh）
- **跳转后悬浮窗**：点击引用跳转文章后，AI问答界面变成左上角悬浮窗，显示"点击返回问答"

#### 4.2.5 新手引导系统
- **聚光灯效果**：全屏变暗，目标区域变亮，金色高亮边框闪烁动画
- **连着播形式**：首次交互后出现目录引导（1/2），点击关闭后自动出现AI搜索引导（2/2）
- **播放器引导独立**：首次播放音频时单独出现（不参与连着播）
- **进度指示**：每个引导顶部显示"1 / 2"、"2 / 2"
- **下一步提示**："点击继续 →"、"点击完成 ✓"、"知道了 ✓"
- **自动消失**：20秒自动消失
- **一次性**：看过后记为localStorage，不再显示

#### 4.2.6 微信引导条
- **检测微信环境**：`/MicroMessenger/i.test(navigator.userAgent)`
- **10秒自动消失**：刚打开时显示，10秒后自动消失，不再弹出
- **z-index**：9998（低于密码输入遮罩层9999）
- **密码输入时不显示**：避免和密码输入界面重叠

#### 4.2.7 密码保护系统
- **所有IP统一密码**：不区分国内国外，所有IP第一次访问都需要输入密码
- **密码**：610
- **密码页面提示**："本站为个人学习使用，非公开用途。访问请输入密码："（两行显示）
- **同一设备记住**：第一次输入后，同一设备再登录不需要输入（localStorage存储）
- **临时链接**：支持创建10分钟失效的免密码临时链接

#### 4.2.8 文章阅读系统
- **上一篇/下一篇**：拉通当前一级目录所有文章，不局限于小分类
- **回到首页/回到分类**：两个按钮，回到分类回到一级根目录
- **按钮样式**：淡色底色，字号较大
- **相关文章推荐**：已移除（用户明确要求去掉）
- **划线功能**：暂未实现，记录为未来功能
- **手机端优化**：左右16px边距，正文两端对齐，基础字号18px

#### 4.2.9 悬浮按钮系统
- **AI搜索按钮**：右下角悬浮，可拖动，靠边吸合隐藏变小
- **播放器按钮**：右下角悬浮，可拖动，靠边吸合隐藏变小
- **字号统一**：0.9rem
- **半透明**：默认半透明，hover时不透明
- **固定定位**：position: fixed，不随页面滚动

---

### 4.3 管理后台系统（admin.html）

**文件位置**：`admin.html`（1160行），构建时复制到`dist/admin/index.html`

**访问地址**：`https://longchen-nyingtik.wiki/admin/`（必须带尾部斜杠）
**管理员口令**：见 §6.3（不在此记录明文；当前口令存于 `Documents\机密-不入云\龙的传人-管理口令-<日期>.txt`，鉴权头 `X-Admin-Token`）

**功能模块**：

#### 4.3.1 访问统计
- **累计IP数**：所有访问IP（不区分国内国外）
- **累计设备数**：以设备ID为主去重
- **最近访问设备**：设备ID、设备名称、设备类型（PC/手机）、最近访问时间、累计访问次数、最近一次访问时长、累计访问时长
- **最近访问IP**：IP地址、省份、城市、最近访问时间、累计访问次数
- **管理员设备过滤**：管理员设备的访问不计入统计（ADMIN_DEVICE_IDS环境变量）
- **访问者分类**：管理员=设备ID列表、爬虫=总停留<60秒、真实用户=总停留≥60秒

#### 4.3.2 文章浏览统计
- **文章打开次数**：每篇文章的访问次数
- **阅读时长**：每篇文章的累计阅读时长（<1分钟忽略不计）
- **管理员设备过滤**：管理员设备的浏览不计入统计

#### 4.3.3 音频播放统计
- **音频播放次数**：每个音频的播放次数
- **播放时长**：每个音频的累计播放时长（<1分钟忽略不计）
- **管理员设备过滤**：管理员设备的播放不计入统计

#### 4.3.4 用户点击分布统计
- **全局点击追踪**：所有可点击元素的点击次数
- **点击最多的元素**：找出用户最关注的功能
- **管理员设备过滤**：管理员设备的点击不计入统计

#### 4.3.5 AI问答统计
- **访问次数**：AI问答功能的使用次数
- **用量**：token消耗量
- **频率**：整体频率、同一用户的频率
- **其他维度**：平均响应时间、成功率、失败原因分布

#### 4.3.6 设备管理
- **设备命名**：允许给设备ID命名（如"我的WinPC"、"我的小米14"）
- **设备类型识别**：PC/手机端、浏览器、操作系统
- **管理员设备标注**：标注哪些是管理员设备

#### 4.3.7 用户注册审核（暂时隐藏）
- **注册页面**：不直接展示，通过单独注册链接访问
- **邀请码注册**：必须使用邀请码，邀请码可多人不限次数使用
- **管理员审核**：注册后必须管理员审核通过才能登录
- **用户功能**：查看自己的记录、发现记录、划线颜色、个人笔记

---

### 4.4 统计与密码验证Worker（stats-auth-worker.js）

**文件位置**：`stats-auth-worker.js`（1343行）
**Worker名称**：`longchen-stats-auth`
**自定义域名**：`https://stats.longchen-nyingtik.wiki`、`https://api.longchen-nyingtik.wiki`
**配置文件**：`wrangler-stats.toml`

**核心功能**：
1. 记录访问统计（设备ID、IP、地理位置、访问时间、停留时长）
2. 密码验证（所有IP统一密码）
3. 用户注册/登录/审核
4. 文章浏览统计
5. 音频播放统计
6. 用户点击分布统计
7. AI问答统计
8. 设备命名管理

**API路由**：
| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/track` | GET/POST | 记录访问，返回访问状态 |
| `/api/stats` | GET | 获取统计状态 |
| `/api/verify-password` | POST | 验证访问密码 |
| `/api/register` | POST | 用户注册（需要邀请码） |
| `/api/login` | POST | 用户登录 |
| `/api/admin/stats` | GET | 管理员获取统计数据 |
| `/api/admin/devices` | GET/POST | 设备管理（命名、标注） |
| `/api/admin/registrations` | GET/POST | 注册审核 |
| `/api/track/article` | POST | 记录文章浏览 |
| `/api/track/audio` | POST | 记录音频播放 |
| `/api/track/click` | POST | 记录用户点击 |
| `/api/track/ai` | POST | 记录AI问答使用 |

**KV存储结构**：
- `device:{deviceId}`：设备信息（IP、地理位置、访问时间、停留时长、名称、类型）
- `ip:{ip}`：IP信息（访问次数、最近访问时间）
- `article:{articleId}`：文章统计（浏览次数、阅读时长）
- `audio:{audioId}`：音频统计（播放次数、播放时长）
- `click:{elementId}`：点击统计（点击次数）
- `user:{userId}`：用户信息（用户名、密码hash、审核状态）
- `ai:{date}`：AI问答统计（按日期存储）

**环境变量**（口令类一律用 Cloudflare Secret，不落文档、不入库）：
- `ACCESS_PASSWORD`：全站访问密码（登录墙）
- `ADMIN_PASSWORD`：管理后台口令（鉴权头 `X-Admin-Token`；本地备份见 §6.3）
- `ADMIN_DEVICE_IDS`：管理员设备ID列表（逗号分隔）

---

### 4.5 AI问答Worker（ai-ask-worker.js）

**文件位置**：`ai-ask-worker.js`（345行）
**Worker名称**：`steep-rain-0d77longchen-ask`
**自定义域名**：`https://ai.longchen-nyingtik.wiki`
**配置文件**：`wrangler.toml`

**核心功能**：
1. 代理调用腾讯云TokenHub的DeepSeek API
2. 隐藏API Key（不暴露给前端）
3. 记录AI问答使用统计（访问次数、token用量、频率）
4. CORS处理（允许跨域请求）

**API路由**：
| 路由 | 方法 | 功能 |
|------|------|------|
| `/api/ask` | POST | 发送问题到DeepSeek API，返回回答 |
| `/api/stats` | GET | 获取AI问答统计 |

**请求格式**：
```json
{
  "question": "用户问题",
  "context": "本站相关文章内容（用于RAG）",
  "deviceId": "设备ID（用于统计）"
}
```

**API配置**：
- **API地址**：`https://tokenhub.tencentmaas.com/v1/chat/completions`
- **模型**：`deepseek-v4-flash-0731`
- **API Key**：存储在Worker环境变量`API_KEY`中（不写在代码里）

**系统提示词**：
- 回答首先基于本站所收集的龙钦宁提相关材料
- 如果本站没有相关内容，会基于网络搜索内容回答，仅供参考
- 回复语气：温暖平和、生活化、有智慧见地，给人希望和信心，不要过于"佛里佛气"

---

### 4.6 Service Worker系统（sw.js）

**文件位置**：`content/sw.js`（源文件），构建时复制到`dist/sw.js`
**缓存版本**：`lct-cache-v3`

**缓存策略**：
| 资源类型 | 策略 | 说明 |
|----------|------|------|
| HTML（index.html） | network-first | 每次都走网络，有更新立即看到 |
| 音频（/audio/*） | cache-first | 文件名即内容指纹，更新即改名，可安全长缓存 |
| 图片 | cache-first | 图片不变，缓存优先 |
| 文章JSON（/pages/*） | stale-while-revalidate | 有缓存先秒开，后台静默更新 |
| AI知识库（knowledge.json） | stale-while-revalidate | 大文件（11.25MB），有缓存先秒开，后台静默更新 |
| CSS/JS | 内联在HTML中 | 随HTML一起更新 |

**特殊功能**：
- Range切片修复：支持音频文件的范围请求（断点续传、拖动进度条）
- 离线支持：缓存过的资源离线可访问
- 缓存清理：版本升级时自动清理旧缓存

---

### 4.7 PWA系统（manifest.json + Service Worker）

**manifest.json配置**：
- **name**：龙的传人 · Longchen Nyingtik
- **short_name**：龙的传人
- **start_url**：`/`
- **display**：`standalone`（独立应用模式）
- **orientation**：`portrait`（竖屏）
- **background_color**：`#f6f1e6`
- **theme_color**：`#8a1f1c`
- **icons**：192x192、512x512、maskable 512x512

**PWA图标**：
- `assets/icon-192.png`：标准图标
- `assets/icon-512.png`：标准图标
- `assets/icon-maskable-512.png`：Android自适应图标（图案在安全区内）
- `assets/apple-touch-icon-180.png`：iOS主屏幕图标
- `assets/favicon-64.png`、`assets/favicon-32.png`：PNG版favicon

**已知问题**：
- 小米浏览器添加到主屏幕后图标可能不显示（已知问题，待优化）
- Chrome浏览器可以正常安装和显示图标

---

## 五、部署流程

### 5.1 完整发布流程
```bash
# 1. 进入项目目录
cd "D:\Users\Drime\Projects\龙的传人-website"

# 2. 构建网站
python build_site.py

# 3. 检查JS语法（可选但推荐）
node -e "const fs=require('fs');const html=fs.readFileSync('dist/index.html','utf8');const r=/<script[^>]*>([\s\S]*?)<\/script>/g;let m,c=0,e=0;while((m=r.exec(html))){if(m[1].trim()){c++;try{new Function(m[1])}catch(err){e++;console.log('Script '+c+' error: '+err.message)}}}console.log(c+' scripts, '+e+' errors')"

# 4. 部署到Cloudflare Pages
npx wrangler pages deploy dist --project-name=longchen-nyingtik --branch=main --commit-dirty=true

# 5. 推送Git备份
git add .
git commit -m "说明"
git push origin v5
```

### 5.2 Worker部署
```bash
# 部署统计Worker
npx wrangler deploy --config wrangler-stats.toml

# 部署AI问答Worker
npx wrangler deploy --config wrangler.toml
```

### 5.3 环境变量设置
```bash
# 设置访问密码
npx wrangler secret put ACCESS_PASSWORD --config wrangler-stats.toml

# 设置管理员密码
npx wrangler secret put ADMIN_PASSWORD --config wrangler-stats.toml

# 设置AI API Key
npx wrangler secret put API_KEY --config wrangler.toml
```

---

## 六、关键ID和配置

### 6.1 Cloudflare配置
- **Account ID**：`36e5618995c36274b4b7b6c7200a2fac`
- **Pages项目名**：`longchen-nyingtik`
- **统计Worker名**：`longchen-stats-auth`
- **AI问答Worker名**：`steep-rain-0d77longchen-ask`
- **KV命名空间**：`STATS_KV`，ID `6b045a39f30a41bcbdd8eb210a174467`

### 6.2 域名配置
- **主域名**：`longchen-nyingtik.wiki`（阿里云购买，Cloudflare DNS）
- **AI问答子域名**：`ai.longchen-nyingtik.wiki`
- **统计/鉴权子域名**：`stats.longchen-nyingtik.wiki`（管理后台 `admin.html` 的 `API_BASE` 即此）
  - ⚠️ `api.longchen-nyingtik.wiki` 已于 2026-09 停用（DNS 记录已不存在）；老脚本/文档若仍写 `api.` 会解析失败
- **管理后台**：`https://longchen-nyingtik.wiki/admin/`（必须带尾部斜杠）

### 6.3 密码配置（2026-09-20 更新）
- ⚠️ **本文档不再记录任何明文口令**（旧版本曾写 `610`／`admin610`，已随 git 历史视同泄露并全部作废）
- **管理员口令（管理后台）**：Cloudflare Secret 名 `ADMIN_PASSWORD`；本地备份存于
  `D:\Users\Drime\Documents\机密-不入云\龙的传人-管理口令-YYYY-MM-DD.txt`（**取文件名日期最新一份**；该目录不入库、不同步）
- **鉴权请求头**：`X-Admin-Token`（统计与管理接口统一使用）
  - ❗ 旧头 `X-Admin-Password` 已失效：Worker 读到空值 → 误报"密码错误"，并白耗防爆破额度
- **访问密码（全站登录墙）**：Cloudflare Secret 名 `ACCESS_PASSWORD`（同样不入本文档）
- **改名/轮换口令的正确姿势**：
  1. `npx wrangler secret put ADMIN_PASSWORD --config wrangler-stats.toml`
  2. 新建 `龙的传人-管理口令-<日期>.txt`（旧文件加"已作废"标记）
  3. 更新交接包 `AiSharedTasks/config.json` 的 `admin_password`
  4. 实测一次：`https://stats.longchen-nyingtik.wiki/api/admin/stats?devices=all` 带 `X-Admin-Token` 应返回 `200`
  5. 在 AI 协作日志登记
- **防爆破**：同一 IP 15 分钟内失败 5 次 → 锁 15 分钟（成功一次即清零计数）

### 6.4 管理员设备ID
- **电脑**：`dev_1788402850264_bbqjsio7d`（已命名"我的WinPC"）
- **手机**：`dev_1788406478720_6fhwoodmy`（已命名"我的小米14"）
- **环境变量**：`ADMIN_DEVICE_IDS=dev_1788402850264_bbqjsio7d,dev_1788406478720_6fhwoodmy`

### 6.5 AI API配置
- **API地址**：`https://tokenhub.tencentmaas.com/v1/chat/completions`
- **模型**：`deepseek-v4-flash-0731`
- **API Key**：存储在Worker环境变量`API_KEY`中

### 6.6 Git配置
- **当前分支**：`v5`
- **远程仓库**：`git@github.com:DrimedNor/longchen-nyingtik.git`
- **GitHub Pages**：已设置redirect分支，访问旧地址自动跳转到新域名

---

## 七、已知问题和待优化项

### 7.1 已知问题
| 问题 | 严重程度 | 说明 |
|------|----------|------|
| ~~管理后台统计数据有时不更新~~ | 已修复(2026-09-06) | 根因确认：/api/admin/stats 串行读取约90次KV，实测13.2秒。已改为两轮Promise.all并行，实测降至1.4-1.8秒 |
| ~~AI问答统计接口未鉴权~~ | 已修复(2026-09-06) | /api/stats/ai-ask 原无需密码即可访问（泄露访客提问）。已纳入统一管理员鉴权 |
| KV免费版写入配额耗尽时统计中断 | 中 | 免费版1000次写/天。2026-09-06已加固：track等统计写入失败静默降级，不再500；根治需第二阶段写入聚合降频 |
| 小米浏览器PWA图标不显示 | 低 | 已知问题，Chrome浏览器正常 |
| 微信分享卡片动态简介不一定100%生效 | 低 | 微信爬虫不一定执行JS，静态meta兜底 |
| AI回答质量不稳定 | 中 | 用户反馈"有AI味道"，已改为搜索与AI分离，按需调用 |
| 锁屏控制在小米自带浏览器可能不显示 | 低 | Media Session API支持有限，Chrome浏览器正常 |
| build_site.py超过5600行 | 中 | 维护成本逐渐增加，中期考虑模块化 |
| 访问者分类大量unknown | 中 | 2026-09-06实测60%设备无visitorType、85%缺UA信息，快照式分类失效。待第二阶段改为读取时动态分类+回填 |

### 7.2 待优化项（按优先级）

#### P0 - 高优先级
1. **用户系统前端开发**：注册页、登录页、用户中心页（后端API已完成）
2. **AI问答优化**：模糊匹配能力、引用来源标注、回复语气调整
3. **管理后台统计数据稳定性**：排查数据不更新的问题

#### P1 - 中优先级
4. **AI知识库瘦身**：11.25MB → 3-4MB，减少首次加载流量
5. **源码模块化**：build_site.py拆分为多文件，产物保持单文件
6. **新手引导"重新观看"功能**：在新手指南页面加一个按钮，方便用户复习
7. **意见反馈入口**：在关于本站加一个反馈入口（微信联系方式或简单表单）
8. **AI问答使用率统计**：在管理后台加一个统计，判断AI功能的真实需求

#### P2 - 低优先级
9. **划线功能**：用户划线保存（需要用户系统支持）
10. **个人笔记功能**：用户在文章中添加个人笔记（需要用户系统支持）
11. **相关文章推荐**：基于标签的相关文章推荐（用户之前要求去掉，未来可重新考虑）
12. **PWA在小米浏览器的图标显示**：排查并修复

### 7.3 内容建设（用户自己做，长期）
1. **上师开示人工校对**：小喇叭🔊标注已校对文章，逐步推进
2. **AI语音生成**：校对完的文章生成AI朗读音频
3. **传承相关书籍补充完善**
4. **传承介绍补充完善**

---

## 八、开发规范和注意事项

### 8.1 硬约束（必须遵守）
1. **法律合规为第一原则**：不传教、不募捐、不组织宗教活动
2. **所有文章标题必须跟Obsidian文件名一模一样**：不要擅自改动
3. **操作文件前必须先备份整个目录**：确认无误后再删除备份
4. **备份文件夹不能放在content/目录下**：不提交推送，只保留本地
5. **assets目录必须排除**：不应该推送到网站
6. **叫"不推送"的目录构建时跳过**：不发布到网站
7. **每次发布网站后必须推送到Git备份**
8. **新增内容一定要跟用户确认后再操作**
9. **用户上传/整理的文件不要轻易删除**：需经确认同意
10. **管理员设备的访问不计入统计**：ADMIN_DEVICE_IDS环境变量过滤

### 8.2 设计规范
1. **手机端左右16px边距**：所有页面不超宽，正文两端对齐
2. **手机端基础字号18px**：文章标题h1不放大
3. **悬浮按钮**：可拖动、靠边吸合隐藏变小、半透明、字号统一（0.9rem）
4. **播放器刷新后不自动弹出面板**
5. **首页一级标题可点击跳转对应分类目录**
6. **上一篇/下一篇拉通当前一级目录所有文章**：不局限于小分类
7. **回到分类回到一级根目录**：不是二级或三级目录
8. **目录数量紧挨着标题名称**
9. **上师开示文章发布顺序严格跟Obsidian文件夹顺序对应**
10. **最近更新列表每一项不要列日期**：只列文章名或音频文件名

### 8.3 AI回复规范
1. **语气**：温暖平和、生活化、有智慧见地，给人希望和信心
2. **禁止**：过于"佛里佛气"、生硬说教、AI味道重
3. **引用来源**：列出引用的文章，点击可跳转
4. **引用过滤**：永远不引用更新日志；上师开示有匹配时不显示三部开示集
5. **本站没有相关内容时**：说明"这个不是基于网站的内容"，再基于大模型知识回答

### 8.4 代码规范
1. **零外部依赖**：不使用React/Vue等框架，纯原生HTML/CSS/JS
2. **单文件SPA**：所有CSS/JS内联到index.html
3. **CSS变量**：使用CSS自定义属性（--accent、--gold等），方便主题切换
4. **JS函数命名**：语义化命名，如`go()`、`showSpotlight()`、`renderAiFallback()`
5. **错误处理**：所有异步操作都要有catch和降级方案
6. **注释**：关键逻辑要有中文注释

---

## 九、外包任务书（已创建，待执行）

### 任务一：用户系统前端开发
- **文件**：`外包任务书-任务一-用户系统前端.md`
- **内容**：注册页、登录页、用户中心页
- **状态**：待执行

### 任务二：AI问答优化
- **文件**：`外包任务书-任务二-AI问答优化.md`
- **内容**：模糊匹配、引用来源、语气调整
- **状态**：待执行

### 任务三：排版统一（可选）
- **文件**：`外包任务书-任务三-排版统一.md`
- **内容**：全站排版统一优化
- **状态**：可选，需确认后再做

---

## 十、联系方式和资源

### 10.1 相关文档
- `项目设计原则与规范.md`：项目设计原则和规范
- `Cloudflare Pages 迁移操作手册.md`：迁移操作手册
- `AI问答部署指南.md`：AI问答部署指南
- `单文件体积优化方案.md`：体积优化方案
- `工作进度看板.md`：工作进度看板
- `项目盘点.md`：项目盘点

### 10.2 关键URL
- **网站首页**：https://longchen-nyingtik.wiki
- **管理后台**：https://longchen-nyingtik.wiki/admin/
- **AI问答API**：https://ai.longchen-nyingtik.wiki
- **统计API**：https://stats.longchen-nyingtik.wiki
- **GitHub仓库**：https://github.com/DrimedNor/longchen-nyingtik
- **Cloudflare Dashboard**：https://dash.cloudflare.com/36e5618995c36274b4b7b6c7200a2fac

---

## 十一、分工说明

### 豆包（整体架构师）
- 负责整体架构设计、产品设计、战略决策
- 负责用户需求分析、产品体验优化
- 负责法律合规风险把控
- 负责Workbuddy交付代码的复核和整合
- 负责建站日志和照见日志的生成

### Workbuddy（执行工程师）
- 负责具体代码编写、技术优化
- 负责外包任务书的执行
- 负责Bug修复、性能优化
- 负责新功能的技术实现
- 交付格式：代码改动整理成Markdown格式，交给豆包复核后装入

---

**文档结束**

> 本文档会持续更新，如有疑问请联系豆包确认。
> 最后更新：2026-09-06
