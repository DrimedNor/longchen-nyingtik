# 项目迁移说明

> 本文档由 Workbuddy 于 2026-08-26 生成，用于「龙的传人」网站项目的迁移/交接。
> 所有路径均为**绝对路径**，从盘符开始。

---

## 1. 项目概览

| 项 | 内容 |
| --- | --- |
| 项目名称 | 龙的传人｜Longchen Nyingtik |
| 定位 | 龙钦宁提（大圆满）传承学习资料库，个人学习整理与分享 |
| 目标读者 | 用户本人 + 身边朋友（非公开运营，无对外推广） |
| 站点地址 | `https://drimednor.github.io/longchen-nyingtik/` |
| 代码仓库 | `git@github.com:DrimedNor/longchen-nyingtik.git`（分支 `v5`） |

**整体状态：**

- **已完成**
  - 静态站骨架（自研 `build_site.py` 单文件 SPA 生成器）
  - 全部内容页（上师开示 37+ 篇、龙钦宁提传承、书籍、关于本站）
  - 音频体系：74 首音频，全局播放器（单曲 / 连播 / 专辑模式 / 倍速 0.5–2x / 播放模式切换 / ±15 秒 / 进度条）
  - 侧栏手风琴折叠导航（默认仅一级展开、多开、展开显示文件夹及音频）
  - 首页「最近更新」公告区、更新日志页
  - 微信域名验证文件
- **进行中**
  - 上师开示音频逐篇补配（当前约 15 篇已配音频，12+ 篇文字版待配）
  - 内容持续更新（每次推送后同步更新「更新日志」与「本次更新内容」）
- **待开发**（详见第 6 节）
  - 搜索中文分词、暗色模式、sw.js 接线、SEO/sitemap/RSS、大音频瘦身等

---

## 2. 技术架构

| 项 | 内容 |
| --- | --- |
| 静态站生成器 | **自研 `build_site.py`**（零第三方依赖，仅 Python 标准库）。历史上有 Quartz 模板残留，但现已完全重写为自研单文件生成器，**不是** Quartz / Hugo / MkDocs |
| 主题 | 完全自研，无外部主题（藏红/浅红 + 金黄 + 暖棕宣纸底） |
| 构建命令 | `python build_site.py`（Python 3.13） |
| 本地预览 | `cd dist && python -m http.server 8000`，或 Workbuddy 的本地预览（present_files 起静态服务） |
| 部署方式 | **GitHub Pages**（`drimednor.github.io/longchen-nyingtik/`） |
| 自定义域名 | **无**（暂未绑定，用户已决定暂不迁移 Cloudflare、维持 GitHub Pages） |
| CI/CD | GitHub Actions `.github/workflows/deploy.yml` |

**构建产物**：`build_site.py` 把 `content/` 下的 Obsidian 风格 markdown 生成**单文件 `dist/index.html`**（内联全部 CSS/JS），并把音频复制到 `dist/audio/`。`dist/` 被 `.gitignore` 忽略，线上由 Actions 每次全新构建。

**CI/CD 流程（deploy.yml）**：
```
push 到 v5 分支
  → actions/setup-python@v5 (Python 3.13)
  → python build_site.py
  → actions/upload-pages-artifact@v3 (上传 dist/)
  → actions/deploy-pages@v4 (部署到 GitHub Pages)
```

**本地部署辅助文件**：
- `cloudflare/_headers`：Cloudflare Pages 缓存规则（已备好，暂未迁移，构建时自动复制到 dist）
- 根目录 `*.txt`（32–64 位 hex 命名）：平台域名验证文件（微信等），构建时自动复制到 dist 根

---

## 3. Obsidian Vault 说明

⚠️ **重要区分**：本项目涉及**两个独立的 markdown 库**，请勿混淆。

### 3.1 网站内容源（`content/` 目录）

这是网站真正的内容库，位于**项目目录内**，是 Obsidian 格式 markdown，但由自研脚本解析（非 Obsidian 打开）。

- **路径**：`D:\Users\Drimed\Projects\龙的传人-website\content\`
- **目录结构**：
  - `index.md` — 首页（frontmatter：`title` + `homepage: true`）
  - `上师开示/` — 上师开示文章，按主题分栏：
    - `1. 为何修行？/` → `1.1 诸行无常`、`1.2 轮回过患`
    - `2. 找到智慧的指引/`
    - `3. 心态决定命运/`
    - `4.如何修行/` → `4.1 正确的依师之道`、`4.2 做一个靠谱的人`
  - `龙钦宁提传承/` — 传承相关资料（祖师家谱、相关网站等）
  - `书籍/` — 书单
  - `关于本站/` — 站点说明
  - `音频资源/` — 音频文件（按专辑文件夹）：
    - `1. 上师开示（AI朗读）/`（15 首）
    - `2. 上师法音/` → `2.1 仪轨与经文`（32 首）、`2.2 圣号与明咒`（24 首）
    - `3. 上师赞歌/`（3 首）
  - `更新日志.md` — 站点版本变更记录（技术变更，不进导航树正文）
  - `本次更新内容.md` — 首页「最近更新」公告区源文件（仅用户资料，不进导航）
  - `assets/`、`sw.js`、`Pasted image 2026*.png` — 辅助资源/未接线文件
- **Frontmatter 字段**（`build_site.py` 的 `parse_frontmatter()` 支持）：
  - `title` — 页面标题（首页同时用作站点名）
  - `tags` — 逗号分隔，转数组展示为标签
  - `author` — 作者（显示「作者：xxx」）
  - `source_url` — 原文链接（显示「查看原文 ↗」）
  - `date` — 日期（`本次更新内容.md` 用于首页公告标题）
  - `homepage: true` — 首页标记（当前 build 实际不读取此字段，仅为标识）
- **特有语法**：
  - 双链 `[[目标]]` / `[[目标|别名]]` — 站内跳转
  - 音频引用 `[[xxx.mp3]]` — 生成内嵌播放按钮
  - 文件名尾缀 `🔊` — 已配音频的文章；`X` — 草稿/未配音频（发布时剥离）
  - **不用** Dataview、**不用** Callout（由自研 `md_to_html` 解析标题/列表/引用/加粗/行内代码/wikilink）

### 3.2 个人知识库「梵心集」（独立，非网站源）

这是用户的个人收藏知识库，与网站**没有直接关系**，仅作为资料来源之一。

- **路径**：`D:\Users\Drime\Documents\Obsidian\梵心集\`
- **分类目录**：`00 实修`、`01_佛法修行`、`02_生活修行`、`03_身心成长`、`04_写作参考`、`05_社会观察`、`06_精华库`、`07_临时收集箱`、`08_智慧栈联络工作`、`09_AI与科技`、`10_上师汇报`、`龙钦宁提`（内含 `微信文章`）、`灵光一闪`、`想法`、`待验证创意`、`得到大脑录音`、`微信读书笔记`、`备忘`
- **标签体系**：快速浏览 / 中等 / 深度研读（深度标签）
- **模板文件**：未发现独立模板目录；收藏/标注通过 Workbuddy 的 `fanxinji-annotation` 技能规范化（带「思考总结」+ 双链互链）
- **已安装插件**：**无社区插件**（无 `community-plugins.json`）；启用的核心插件含 file-explorer、global-search、graph、backlink、canvas、tag-pane、properties、daily-notes、templates、note-composer、command-palette、bookmarks 等
- **双链使用**：启用 backlink/outgoing-link，文章间用 `[[...]]` 双向互链

---

## 4. 样式与设计

| 项 | 内容 |
| --- | --- |
| 配色 | 暖棕「宣纸」底色 + 藏红主色 + 金黄辅助（佛法庄严感）。详见下方色值 |
| 字体 | 系统默认字体（未引入自定义 webfont） |
| CSS 位置 | 全部**内联在 `build_site.py` 的 `PAGE_TEMPLATE` 字符串**里（单文件 HTML，无外部 CSS） |
| Logo / Favicon | **无**（未设置站点图标） |

**配色方案（`:root` CSS 变量，位于 `build_site.py` 第 310 行起）**：

| 用途 | 变量 | 色值 |
| --- | --- | --- |
| 页面主背景（宣纸奶白） | `--bg` | `#f6f1e6` |
| 卡片面板（暖白） | `--surface` | `#fffdf8` |
| 主文字（沉静深棕） | `--ink` | `#3b2a22` |
| 主色（深红·护法庄严） | `--accent` | `#8a1f1c` |
| 辅助色（金黄·尊贵） | `--gold` | `#b8893b` |
| 强调色（绿松石·清净） | `--turq` | `#2f9b8f` |
| 强调色（靛蓝·沉静） | `--indigo` | `#2f3a5c` |

**主要自定义 CSS 修改点**（均在 `build_site.py` 内）：
- 手风琴折叠导航样式（`.nav-sec` / `.nav-sec-head` / `.nav-sec-body` / `.nav-audio-list`）
- 播放器样式（`.player` / 半屏 + 底部迷你条 `.player-mini` / 倍速菜单 / 专辑菜单）
- 音频条目卡片（`li:has(.play-btn)`）、「音频制作中」角标
- 首页「最近更新」区块、导览板块

---

## 5. 已配置功能

| 功能 | 实现方式 | 状态 |
| --- | --- | --- |
| 站内搜索 | 前端 JS 简单过滤（`title` + `html` 的 `indexOf` 匹配） | 已上线，**无中文分词** |
| 音频播放器 | 全局单实例播放器：单曲/连播、**专辑模式**（按文件夹分类）、倍速（0.5–2x）、播放模式（顺序/逆序/随机/单曲）、±15 秒、进度条、底部迷你条 | 已上线 |
| 手风琴折叠导航 | 侧栏 `nav-sec` 结构，默认仅一级展开、多开、展开显示文件夹及所属音频、可边栏直接播放 | 已上线 |
| 首页公告区 | `本次更新内容.md` 驱动「最近更新 · 日期」区块（仅当次新增、不累积历史） | 已上线 |
| 更新日志 | `更新日志.md`（按天记录、分级标签） | 已上线 |
| 访问统计 | `api.countapi.xyz` 免注册计数，仅管理员密钥（`?admin=`）可见 | 已上线 |
| 评论系统 | 无 | — |
| 暗色模式 | **无** | 未做 |
| Service Worker（PWA 缓存） | `content/sw.js` **已写但未接线**（未复制到 dist、未注册） | 待接线 |
| 微信域名验证 | 根目录 `2d960586b79134c57aeb345252956b79.txt`，构建自动复制到 dist 根 | 已上线 |
| Cloudflare 缓存规则 | `cloudflare/_headers` 已备好（HTML no-cache / 音频长缓存） | 暂未迁移 |

---

## 6. 待完善事项（重点）

按优先级排列，尽量具体：

### 高优先级

1. **搜索不支持中文分词**
   现状：搜索是 `title`/`html` 的 `indexOf` 子串匹配，无法按词搜索（如搜「无常」搜不到「诸行无常」的分词，实际能命中子串但无同义词/分词扩展）。希望接入中文分词（如前端 `jieba` 的 wasm 版，或 lunr.js 的中文配置），并加高亮。

2. **Service Worker 已写未接线**
   `content/sw.js` 已实现（音频 cache-first 离线重听、其他资源 stale-while-revalidate），但 `build_site.py` **没有**把它复制到 `dist/`，index.html 也没有 `navigator.serviceWorker.register`。需：① build 时复制 `sw.js` 到 dist 根；② 模板里注册 serviceWorker；③ 注意 `_headers` 与 sw 缓存策略的配合。

3. **暗色模式未做**
   配色只有浅色（宣纸暖白底）。希望增加暗色主题（`prefers-color-scheme` 或手动切换），配色延续「藏红+金黄」在深底下的可读性。

### 中优先级

4. **大音频文件逼近 GitHub 限制**
   `早课（49分钟）.mp3` 已 67.5MB（> GitHub 建议的 50MB），推送时有警告但未超 100MB 硬限制。未来新增大音频可能超限导致 push 失败。建议：① 引入 Git LFS；② 或压缩/拆分音频；③ 或把大音频放独立对象存储（OSS/网盘）+ 站内放外链。

5. **更新后用户端缓存旧版问题**
   GitHub Pages 无自定义域名无法控制 CDN 缓存，推送后用户可能看到旧版（已多次发生）。当前靠「提醒用户硬刷新/无痕」。根治方案是迁移 Cloudflare Pages（`_headers` 已备好）——但用户已决定暂缓，后续如需可随时启用。

6. **SEO 缺失**
   无 `sitemap.xml`、无 `robots.txt`、无 `<meta name="description">`、无 Open Graph / Twitter Card。因是 SPA（内容内联 JS），搜索引擎收录极差。建议至少补 `robots.txt` + `sitemap.xml` + 基础 meta。

7. **无 RSS 订阅**，可考虑生成 `feed.xml`（对佛法资料读者友好）。

8. **无站点图标（favicon）**，浏览器标签页是默认图标。

### 低优先级

9. **历史遗留大文件瘦身**
   仓库历史曾含 56.75MB《做一个真正的修行人》PDF 对象（已不在工作树，但在 git 历史里）。彻底瘦身需 `git filter-repo` + 强推，需用户拍板后执行。

10. **12+ 篇文字版上师开示待逐篇配音频**（当前只上了文字版，正文有「音频制作中」角标）。

11. **`content/assets/audio/` 空目录残留**、`content/Pasted image 2026*.png` ×3 未归档。

### 后续优先方向建议
- 优先做「搜索分词」与「sw.js 接线」（体验提升最直接）
- 其次「暗色模式」与「大音频瘦身」（可持续性）
- SEO/sitemap/RSS 视是否要对外公开而定（当前仅自用+朋友，优先级可放后）

---

## 7. 参考与对标

- **欣赏的同类网站参考链接**：待用户补充（Workbuddy 记忆中无用户明确记录的对标站点；站内当前仅有一处昌列寺有声书外链 `http://www.changleisi.com/index/Audio/details/id/71`）。
- **希望避免的问题**：
  - 避免依赖第三方前端框架/CDN（保持零依赖、单文件、离线可用）
  - 避免 mp3 文件名含弯引号 `“ ”`（GitHub Pages 静态资源 URL 会 404，历史踩坑）
  - 避免把「本次更新内容」与「更新日志」混为一谈（前者=用户资料公告，后者=技术变更记录）
  - 避免「云扬」等 TTS 音色名误当「上师」（本站上师唯一指龙洋仁波切）

---

## 附：本地绝对路径清单（核实后）

| 序号 | 项目 | 真实绝对路径 |
| --- | --- | --- |
| 1 | 静态网站项目目录（GitHub 仓库本地克隆） | `D:\Users\Drimed\Projects\龙的传人-website\` |
| 2 | 网站内容源（Obsidian 格式 markdown 库） | `D:\Users\Drimed\Projects\龙的传人-website\content\` |
| 3 | 构建脚本 | `D:\Users\Drimed\Projects\龙的传人-website\build_site.py` |
| 4 | 本迁移说明文档 | `D:\Users\Drimed\Projects\龙的传人-website\项目迁移说明.md` |
| 5 | 个人知识库 Vault「梵心集」 | `D:\Users\Drime\Documents\Obsidian\梵心集\` |
| 6 | 梵心集·龙钦宁提资料 | `D:\Users\Drime\Documents\Obsidian\梵心集\龙钦宁提\` |
| 7 | Workbuddy 项目工作记录（日记 + 长期约定） | `D:\Users\Drimed\SynologyDrive\Workbuddy\2026-08-09-11-34-10\.workbuddy\memory\` |
| 8 | Workbuddy 用户级长期记忆 | `C:\Users\Drime\.workbuddy\MEMORY.md` |

> 说明：清单里的示例路径（`C:\Users\Drime\Documents\佛学Vault`、`C:\Users\Drime\Projects\foxue-site`）是模板占位，与真实项目不符，已按实际核实结果更正。
