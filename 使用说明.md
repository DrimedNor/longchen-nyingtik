# 龙的传人 —— Obsidian + Quartz + GitHub Pages 使用说明

> 最后更新：2026-08-10

---

## 一、项目概览

**网站名称**：龙的传人  
**副标题**：龙钦宁提文化资料库  
**线上地址**：https://drimednor.github.io/long-de-chuanshen/  
**GitHub 仓库**：https://github.com/DrimedNor/long-de-chuanshen  

### 技术架构

```
Obsidian（写作工具）
    ↓  手动复制发布内容
Quartz v5（静态网站生成器）
    ↓  Git push
GitHub Actions（自动构建）
    ↓
GitHub Pages（网站托管）
```

---

## 二、项目目录结构

### Obsidian Vault（原始资料库）

```
D:\Users\Drimed\Documents\Obsidian\龙的传人\
├── 00-网站首页/          ← 首页相关页面
├── 01-上师开示/          ← 上师开示（按主题分类）
│   ├── 发心/
│   ├── 菩提心/
│   ├── 前行/
│   ├── 依止善知识/
│   ├── 修行次第/
│   ├── 大圆满/
│   ├── 日常修行/
│   └── 问答开示/
├── 02-传承祖师/          ← 祖师传记与资料
├── 03-经典论典/          ← 经典译本与论典
├── 04-修学主题/          ← 按主题组织的修学资料
├── 05-历史与传播/        ← 历史渊源与传播历程
├── 06-学习笔记/          ← 个人学习心得
├── 07-音频资源/          ← 文字转语音音频
├── 99-私人资料/ 🔒       ← 永远不会发布（Obsidian 专属）
└── templates/            ← Obsidian 模板
```

### Quartz 网站项目

```
D:\Users\Drimed\Projects\龙的传人-website\
├── content/              ← 网站内容（只放准备发布的内容）
│   ├── index.md          ← 首页
│   ├── 上师开示/
│   ├── 传承祖师/
│   ├── ...
│   └── assets/           ← 图片、音频等资源
├── quartz.config.yaml    ← Quartz 配置
├── .github/workflows/
│   └── deploy.yml        ← GitHub Pages 自动部署配置
└── public/               ← 构建输出（自动生成，不手动修改）
```

### 关键安全边界

| 目录 | 作用 | 是否公开 |
|------|------|----------|
| `Obsidian/龙的传人/` | 原始写作资料库 | ❌ 不公开 |
| `Quartz/content/` | 手动选入的发布内容 | ✅ 会公开 |
| `Quartz/public/` | 构建生成的网站文件 | ✅ 自动部署 |
| `Obsidian/99-私人资料/` | 私人笔记 | ❌ 永远不进网站 |

---

## 三、日常工作流

### 1. 写文章

```
打开 Obsidian → 在对应分类文件夹新建笔记 → 撰写内容
```

- 使用 `[[页面名]]` 创建内部链接
- 使用 `#标签名` 添加标签
- 图片拖入 Obsidian 即可

### 2. 准备发布

当一篇文章准备公开时：

1. 将 Markdown 文件从 Obsidian vault **复制**到 Quartz 的 `content/` 对应目录
2. 如果文章引用了图片，也一并复制到 `content/assets/` 目录
3. 如果是上师开示的文章想要附带音频：
   - 将 MP3 文件放入 `content/assets/audio/`
   - 在文章中使用 `<audio controls><source src="assets/audio/文件名.mp3" type="audio/mpeg"></audio>` 嵌入

### 3. 发布到网站

```bash
# 进入 Quartz 项目目录
cd "D:\Users\Drimed\Projects\龙的传人-website"

# 添加所有更改
git add -A

# 提交
git commit -m "发布：新增XXX文章"

# 推送到 GitHub（自动触发构建和部署）
git push origin v5
```

推送后约 1-2 分钟，网站自动更新。

### 4. 本地预览

如果需要在发布前预览效果：

```bash
cd "D:\Users\Drimed\Projects\龙的传人-website"
npx quartz build
# 构建完成后，public/ 目录就是网站文件
# 可以用浏览器直接打开 public/index.html 查看
```

---

## 四、内容编写指南

### Obsidian 双链语法

```markdown
[[上师开示/发心/index]]      → 链接到发心页面
[[测试页面A]]                 → 链接到同目录文件
[[测试页面B|点击这里]]        → 自定义显示文字
```

### 标签

在文件头部 YAML 添加：

```yaml
---
tags:
  - 上师开示
  - 菩提心
---
```

### Callout（提示框）

```markdown
> [!note] 标题
> 内容

> [!warning] 注意
> 重要提醒内容
```

### 嵌入音频

```markdown
<audio controls>
  <source src="assets/audio/文件名.mp3" type="audio/mpeg">
  您的浏览器不支持音频播放。
</audio>
```

> ⚠️ 音频**不会**自动播放，需要手动点击。`autoplay` 属性已被禁用。

---

## 五、如何防止私人资料误发布

以下机制保证了安全性：

1. **物理隔离**：Obsidian vault 和 Quartz 项目是两个独立目录
2. **手动选择**：只有手动复制到 `Quartz/content/` 的文件才会进入网站
3. **Git Ignore**：`public/` 目录不会被提交（自动生成）
4. **Quartz 忽略模式**：`quartz.config.yaml` 中已配置忽略 `private`、`templates`、`.obsidian`

### 安全清单

- [ ] 发布前检查文章是否包含个人信息（姓名、电话、地址等）
- [ ] 图片是否合适公开
- [ ] 私人笔记是否放在了 `99-私人资料/` 目录
- [ ] Quartz `content/` 目录不会误入私人文件

---

## 六、常见操作

### 新增文章分类

1. 在 Obsidian vault 和 Quartz `content/` 中同时创建新目录
2. 在目录中创建 `index.md` 作为分类首页
3. 在 `content/index.md` 中添加导航链接

### 更新已有文章

1. 在 Obsidian 中修改文章
2. 将修改后的文件重新复制到 Quartz `content/` 目录（覆盖旧文件）
3. Git commit + push

### 备份

- Obsidian vault 已有 Synology Drive 自动同步备份
- GitHub 仓库作为网站内容和代码的备份
- **建议**：定期将 `Obsidian/龙的传人/` 中已发布的内容手动同步到 Quartz

---

## 七、常见错误及解决

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 网站 404 | GitHub Pages 部署失败 | 检查 GitHub Actions 日志 |
| 中文链接打不开 | 文件名编码问题 | Quartz v5 原生支持中文，无需额外配置 |
| 双向链接不显示 | 文件未在 content 目录 | 确认两个文件都在 `content/` 中 |
| 图片显示不了 | 图片路径不对 | 使用相对路径如 `assets/图片.jpg` |
| 音频不播放 | 浏览器不支持 | 使用 Chrome/Edge；检查 MP3 路径 |
| 本地预览空白 | 未执行构建 | 先运行 `npx quartz build` |

---

## 八、配置文件说明

### quartz.config.yaml（关键配置）

| 配置项 | 当前值 | 说明 |
|--------|--------|------|
| `pageTitle` | 龙的传人 | 网站标题 |
| `baseUrl` | drimednor.github.io/long-de-chuanshen | 网站 URL |
| `locale` | zh-CN | 中文界面 |
| `ignorePatterns` | private, templates, .obsidian | 自动忽略的目录 |

---

## 九、技术栈版本

| 组件 | 版本 |
|------|------|
| Quartz | v5.0.0 |
| Node.js | >= 22 |
| 部署方式 | GitHub Actions + GitHub Pages |
| 构建分支 | v5 |

---

## 十、更新 Quartz

当 Quartz 发布新版本时：

```bash
cd "D:\Users\Drimed\Projects\龙的传人-website"
git fetch upstream
git merge upstream/v5
npm ci
npx quartz plugin install
```

> 首次设置后，Quartz 的 upstream 远程已自动配置。

---

> 💡 **核心理念**：Obsidian 是你的私人花园，Quartz 是你选择对外展示的橱窗。你在花园里自由生长，只把精选出来的放进橱窗。
