# 龙的传人 —— Obsidian + Quartz + GitHub Pages 使用说明

> 最后更新：2026-08-10 下午（修复音频内联播放器 + 首页自动目录树）

---

## 一、项目概览

**网站名称**：龙的传人  
**副标题**：龙钦宁提文化资料库  
**线上地址**：https://drimednor.github.io/longchen-nyingtik/  
**GitHub 仓库**：https://github.com/DrimedNor/longchen-nyingtik  

### 技术架构

```
Obsidian（写作工具，直接打开 Quartz 的 content/ 目录即可）
    ↓  Git 提交 + 推送
Quartz v5（静态网站生成器，自动构建）
    ↓  GitHub Actions
GitHub Pages（网站托管）
```

---

## 二、项目目录结构

### Obsidian Vault（原始资料库）

```
D:/Users/Drimed/Documents/Obsidian/龙的传人/
├── 上师开示/             ← 上师开示（按主题分类，如 01 为何修行/）
├── 音频资源/             ← 文字转语音音频
├── 99-私人资料/  ← 永远不会发布（Obsidian 专属）
└── templates/            ← Obsidian 模板

> 更简单的做法：直接将 Quartz 的 `content/` 目录作为 Obsidian Vault 打开，
> 在里边写作即等于发布内容，无需在多个 Vault 之间手动复制（推荐）。
```

### Quartz 网站项目

```
D:\Users\Drimed\Projects\龙的传人-website\
├── content/              ← 网站内容（只放准备发布的内容）
│   ├── index.md          ← 首页
│   ├── 上师开示/          ← 上师开示（按主题分类）
│   ├── 音频资源/          ← 文字转语音音频 + 对应文章
│   └── assets/           ← 图片等静态资源（⚠️ 放此目录的 .md 不会发布）
├── quartz.config.yaml    ← Quartz 配置（含 ContentTree 组件注册）
├── quartz/components/Head.tsx  ← 含音频播放器脚本（自动把 [[mp3]] 变 ▶）
├── node_modules/@quartz-community/local-content-tree/  ← 本地自建组件（首页目录树，⚠️ 不进 git，见下方"本地组件恢复"）
├── .github/workflows/
│   └── deploy.yml        ← GitHub Pages 自动部署配置
└── public/               ← 构建输出（自动生成，不手动修改）
```

### 关键安全边界

| 目录 | 作用 | 是否公开 |
|------|------|----------|
| `Obsidian/龙的传人/` | 原始写作资料库 | ❌ 不公开 |
| `Quartz/content/` | 手动选入的发布内容 | ✅ 会公开 |
| `Quartz/public/` | 构建生成的网站文件 | ✅ 自动部署 |
| `Obsidian/99-私人资料/` | 私人笔记 | ❌ 永远不进网站 |

---

## 三、日常工作流

### 1. 写文章

```
打开 Obsidian → 在对应分类文件夹新建笔记 → 撰写内容
```

- 使用 `[[页面名]]` 创建内部链接
- 使用 `#标签名` 添加标签
- 图片拖入 Obsidian 即可

### 2. 准备发布

当一篇文章准备公开时：

> 推荐：直接用 Obsidian 打开 `Quartz/content/` 目录作为 Vault 写作，写的就是发布内容，无需复制。
> 若从其他 Vault 整理内容过来：把 Markdown 复制到 `content/` 对应目录，图片复制到 `content/assets/`。

3. 如果文章想要附带音频（文字转语音后）：
   - 将 MP3 文件放入 `content/音频资源/` 目录（或 content 下任意目录）
   - 在文章中直接用 `[[文件名.mp3]]` 即可 —— 网站会自动渲染成 ▶ 播放器

### 3. 发布到网站

```bash
# 进入 Quartz 项目目录
cd "D:\Users\Drimed\Projects\龙的传人-website"

# 添加所有更改
git add -A

# 提交
git commit -m "发布：新增XXX文章"

# 推送到 GitHub（自动触发构建和部署）
git push origin v5
```

推送后约 1-2 分钟，网站自动更新。

### 4. 本地预览

如果需要在发布前预览效果：

```bash
cd "D:\Users\Drimed\Projects\龙的传人-website"
npx quartz build
# 构建完成后，public/ 目录就是网站文件
# 可以用浏览器直接打开 public/index.html 查看
```

---

## 四、内容编写指南

### Obsidian 双链语法

```markdown
[[上师开示/发心/index]]      → 链接到发心页面
[[测试页面A]]                 → 链接到同目录文件
[[测试页面B|点击这里]]        → 自定义显示文字
```

### 标签

在文件头部 YAML 添加：

```yaml
---
tags:
  - 上师开示
  - 菩提心
---
```

### Callout（提示框）

```markdown
> [!note] 标题
> 内容

> [!warning] 注意
> 重要提醒内容
```

### 嵌入音频（自动播放器）

在文章中**直接写**音频文件名即可，网站会自动变成可点击播放的播放器：

```markdown
[[万事无常，唯有佛法是依靠.mp3]]
```

- 不需要写 HTML `<audio>` 标签
- 不需要加 `!` 前缀（普通 `[[ ]]` 链接语法即可）
- 网站内置脚本会自动把音频链接替换为 `<audio controls>` 播放器
- 音频文件放在 `content/音频资源/` 或 content 下任意位置均可
- 播放器**不会**自动播放，需手动点击 ▶（尊重流量与体验）
- 播放器直接在**文章页内**显示，无需跳转单独页面（之前是超链接样式，已修复为内联 `<audio>`）

### 首页内容导航（自动目录树）

首页的"📖 内容导航"由网站自动生成，**无需手动维护**：

- 组件 `ContentTree` 读取所有已发布页面，按文件夹层级自动构建目录树
- 显示一级 / 二级 / 三级文件夹 + 文章名，均可点击直接跳转
- 新增文章或分类后，首页目录树**自动更新**，无需改 `index.md`
- 目录树仅在首页显示（`content/index.md` 需带 `homepage: true` frontmatter）

---

## 五、如何防止私人资料误发布

以下机制保证了安全性：

1. **物理隔离**：Obsidian vault 和 Quartz 项目是两个独立目录
2. **手动选择**：只有手动复制到 `Quartz/content/` 的文件才会进入网站
3. **Git Ignore**：`public/` 目录不会被提交（自动生成）
4. **Quartz 忽略模式**：`quartz.config.yaml` 中已配置忽略 `private`、`templates`、`.obsidian`

### 安全清单

- [ ] 发布前检查文章是否包含个人信息（姓名、电话、地址等）
- [ ] 图片是否合适公开
- [ ] 私人笔记是否放在了 `99-私人资料/` 目录
- [ ] Quartz `content/` 目录不会误入私人文件

---

## 六、常见操作

### 新增文章分类

1. 在 Obsidian vault 和 Quartz `content/` 中同时创建新目录
2. 在目录中创建 `index.md` 作为分类首页
3. 在 `content/index.md` 中添加导航链接

### 更新已有文章

1. 在 Obsidian 中修改文章
2. 将修改后的文件重新复制到 Quartz `content/` 目录（覆盖旧文件）
3. Git commit + push

### 备份

- Obsidian vault 已有 Synology Drive 自动同步备份
- GitHub 仓库作为网站内容和代码的备份
- **建议**：定期将 `Obsidian/龙的传人/` 中已发布的内容手动同步到 Quartz

---

## 七、常见错误及解决

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 网站 404 | GitHub Pages 部署失败 | 检查 GitHub Actions 日志 |
| 中文链接打不开 | 文件名编码问题 | Quartz v5 原生支持中文，无需额外配置 |
| 双向链接不显示 | 文件未在 content 目录 | 确认两个文件都在 `content/` 中 |
| 图片显示不了 | 图片路径不对 | 使用相对路径如 `assets/图片.jpg` |
| 音频不播放 | 浏览器不支持 | 使用 Chrome/Edge；检查 MP3 路径 |
| 本地预览空白 | 未执行构建 | 先运行 `npx quartz build` |

---

## 八、配置文件说明

### quartz.config.yaml（关键配置）

| 配置项 | 当前值 | 说明 |
|--------|--------|------|
| `pageTitle` | 龙的传人 | 网站标题 |
| `baseUrl` | drimednor.github.io/longchen-nyingtik | 网站 URL |
| `locale` | zh-CN | 中文界面 |
| `ignorePatterns` | private, templates, .obsidian | 自动忽略的目录 |

---

## 九、技术栈版本

| 组件 | 版本 |
|------|------|
| Quartz | v5.0.0 |
| Node.js | >= 22 |
| 部署方式 | GitHub Actions + GitHub Pages |
| 构建分支 | v5 |

---

## 十、更新 Quartz

当 Quartz 发布新版本时：

```bash
cd "D:\Users\Drimed\Projects\龙的传人-website"
git fetch upstream
git merge upstream/v5
npm ci
npx quartz plugin install
```

> 首次设置后，Quartz 的 upstream 远程已自动配置。

---

## 十一、本地组件恢复（重要）

首页目录树组件 `ContentTree` 放在 `node_modules/@quartz-community/local-content-tree/`，**被 .gitignore 忽略，不进 git 仓库**。

如果 `node_modules` 被重建（`npm install` / 换机器 clone 后），这个组件会丢失，构建会报 "Plugin not found" 错误。恢复步骤：

**1. 创建 package.json**
```
node_modules/@quartz-community/local-content-tree/package.json
```
内容：
```json
{
  "name": "@quartz-community/local-content-tree",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": { "import": "./dist/index.js" },
    "./components": { "import": "./dist/components/index.js" },
    "./package.json": "./package.json"
  },
  "main": "./dist/index.js",
  "quartz": {
    "name": "local-content-tree",
    "displayName": "Content Tree",
    "category": "component",
    "components": {
      "ContentTree": { "displayName": "Content Tree", "defaultPosition": "afterBody", "defaultPriority": 5 }
    }
  }
}
```

**2. 创建 components.tsx 并编译**
```bash
cd "D:\Users\Drimed\Projects\龙的传人-website"
mkdir -p node_modules/@quartz-community/local-content-tree
# 写入 components.tsx（外层工厂结构）
npx esbuild node_modules/@quartz-community/local-content-tree/components.tsx \
  --bundle --format=esm --platform=node --packages=external \
  --jsx=automatic --jsx-import-source=preact \
  --outfile=node_modules/@quartz-community/local-content-tree/dist/components/index.js
rm node_modules/@quartz-community/local-content-tree/components.tsx
```

`components.tsx` 核心结构（组件必须是外层工厂 `() => Component`）：
```tsx
import type { QuartzComponent, QuartzComponentProps } from "@quartz-community/types"
import { resolveRelative } from "../../../quartz/util/path"

// ... buildTree / renderNode 函数（见仓库历史提交 06e9c14）...

const Component = (props: QuartzComponentProps) => {
  const { fileData, allFiles } = props
  const fm = fileData.frontmatter as any
  if (!fm || !fm.homepage) return null
  const root = buildTree(allFiles as any)
  return (
    <nav class="content-tree">
      <h2>📖 内容导航</h2>
      {renderNode(root, (fileData.slug as string) ?? "")}
    </nav>
  )
}
Component.css = `/* 见 06e9c14 提交 */`
export const ContentTree = () => Component
export default ContentTree
```

**3. 重新构建**
```bash
rm -rf public && npx quartz build
```

> ⚠️ 注意：Windows 下 Quartz 本地插件不能用 `./相对路径` 的 source（symlink 权限失败 EPERM），必须放 `node_modules/@quartz-community/` 下当已安装包引用。

---

> 💡 **核心理念**：Obsidian 是你的私人花园，Quartz 是你选择对外展示的橱窗。你在花园里自由生长，只把精选出来的放进橱窗。
