# Cloudflare Pages 响应头策略
# 2026-09-12 变更：本站改为「登录后才可访问」，全站禁止一切搜索引擎收录。
#   - /* 统一加盖 X-Robots-Tag: noindex, nofollow（对匿名与已登录访客一视同仁）
#   - 缓存策略调整：HTML/首页改为不缓存，保证访问门槛即时生效
# 注：本文件由 build_site.py 复制到 dist/ 根；函数（functions/*）动态生成的响应
#     不经过本文件，其 noindex 头由 _middleware.js 自行设置。

# 1) 全站禁止收录 + 基础安全头（放在最前，后续规则只覆盖缓存类头，不覆盖本头）
# 2026-09-18 补安全头：CSP（本站为单文件内联 JS/CSS，故 script/style 允许 inline）
# 2026-09-21 修正（⚠️ 重要回归）：connect-src 必须放行统计 Worker 域。
#   9-18 那次把 connect-src 收敛为 'self' 时，误以为浏览器侧已不再外联 Worker，
#   但两处仍在跨源调用 stats.longchen-nyingtik.wiki：
#     ① admin.html（API_BASE）——后台登录/统计/审核/一次性登记链接全部被 CSP 拦掉，
#        表现为「后台登不上」「打开登记链接没反应」，且服务端无任何异常日志（curl 不执行 CSP）；
#     ② build_site.py 注入的主站埋点（identify-device / device-info / click / duration / page-view）。
#   故这里显式放行该域；除它之外仍保持 'self'。
#   ⚠️ 新增任何浏览器侧跨源接口时，必须同步在此加域，否则会被静默拦截。
/*
  X-Robots-Tag: noindex, nofollow, noarchive, nosnippet, noimageindex
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer
  X-Frame-Options: DENY
  Strict-Transport-Security: max-age=31536000; includeSubDomains
  Permissions-Policy: camera=(), microphone=(), geolocation=(), interest-cohort=()
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; media-src 'self' blob:; connect-src 'self' https://stats.longchen-nyingtik.wiki; font-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'

# 2) 静态资源缓存策略（2026-09-19 2-1 C+ 实施：媒体全量 no-store——
#    修复「边缘缓存命中时 Pages Function 不执行」的匿名可读漏洞；代价＝媒体请求全部回源鉴权）。
#    图标类（icon/manifest 等无内容敏感性）同样 no-store：数量少（约 10 张），统一处理减少规则面。
/assets/*
  Cache-Control: private, no-store

/audio/*
  Cache-Control: private, no-store

# 3) HTML 页面与首页不缓存（保证门槛与内容更新即时生效）
/index.html
  Cache-Control: no-store

/
  Cache-Control: no-store

# 4) robots.txt 缓存 1 小时
/robots.txt
  Cache-Control: public, max-age=3600

# 5) Service Worker 不缓存（保证更新及时）
/sw.js
  Cache-Control: no-cache, no-store, must-revalidate
