# 「龙的传人」网站 · 藏传佛教主题 UI 配色方案

> 设计取向：从藏传佛教的文化内涵与视觉传统出发，构建一套宁静、神圣、庄重的宗教美学。
> 象征体系参考：金黄（庄严·尊贵）、白/奶白（慈悲·清净）、深红（护法·能量）、靛蓝（沉静·护法）、绿松石（清净·智慧）。
> 本文档为 `build_site.py` 中 `:root` 变量与各组件的色彩规范来源，改动时以本文为准。

---

## 一、象征色体系与搭配逻辑

| 象征色 | HEX | 宗教内涵 | 在界面中的角色 |
| --- | --- | --- | --- |
| 深红 Deep Red | `#8a1f1c` | 护法、庄严、能量 | **主色**：链接、主按钮、当前激活态、关键强调 |
| 金黄 Gold | `#b8893b` | 尊贵、庄严、佛光 | **辅助色**：描边、修饰线、标题强调、播放器顶饰 |
| 绿松石 Turquoise | `#2f9b8f` | 清净、智慧、供养 | **强调色**：进度条填充、播放中指示点 |
| 靛蓝 Indigo | `#2f3a5c` | 沉静、护法、虚空 | 次级强调：链接悬停、冷色对比 |
| 奶白/宣纸 Ivory | `#f6f1e6` | 慈悲（白色）、清净 | 背景基底，如寺院素墙 |
| 暖深棕 Ink | `#3b2a22` | 沉静、可读 | 主文字与标题 |

**搭配逻辑（从主到次）：**
1. **主色 深红** 承担一切"可执行/可点击/当前态"的视觉重量，是全站最醒目的色。
2. **辅助色 金黄** 不做大面积填充，只作"镶边"——顶栏下缘、播放器顶饰、卡片描边、激活态内阴影，呼应寺院金顶与唐卡描金。
3. **强调色 绿松石** 仅用于"进度/进行中"这类动态反馈，使清净之意随播放流动。
4. **靛蓝** 作为深红的冷色伙伴，用于链接悬停等次级场景，提供沉静对比。
5. **奶白背景 + 暖深棕文字** 构成温润、低刺激的纸本阅读基调，保证长时间阅读不疲劳。

---

## 二、色彩 Token（`:root`）

```css
:root{
  /* 背景：奶白 / 宣纸（白色=慈悲） */
  --bg:#f6f1e6;          /* 页面主背景 */
  --bg-2:#efe7d6;        /* 区块交替底色 */
  --surface:#fffdf8;     /* 卡片/面板表面 */
  --surface-soft:#f0e8d6;/* 引用/标签/选中态 */
  --surface-hover:#ebe1cb;/* 悬停态 */

  /* 文字层级（暖深棕，保证可读性） */
  --ink:#3b2a22;         /* 主文字 / 标题 */
  --ink-soft:#6a4f43;    /* 次级文字 */
  --ink-faint:#9b8475;   /* 提示 / 面包屑 / 元信息 */

  /* 线条 */
  --line:#e2d6bf;        /* 普通分割线 */
  --line-strong:#cbb893; /* 强调分割线（金褐） */

  /* 主色：深红（护法·庄严） */
  --accent:#8a1f1c;
  --accent-soft:#b0473f;
  --accent-deep:#6e1614;

  /* 辅助色：金黄（尊贵·庄严） */
  --gold:#b8893b;
  --gold-soft:#d9b86a;
  --gold-deep:#8a6320;

  /* 强调色：绿松石（清净） */
  --turq:#2f9b8f;
  --turq-soft:#5bb8ad;

  /* 靛蓝（沉静·护法） */
  --indigo:#2f3a5c;
  --indigo-soft:#4a577e;

  --fs:1rem;
}
```

---

## 三、核心 UI 组件色彩规范

### 1. 背景与文字
- 页面背景 `--bg` 奶白；区块交替用 `--bg-2`。
- 正文 `--ink` 暖深棕，次级 `--ink-soft`，提示/元信息 `--ink-faint`。
- 对比度：深红 `#8a1f1c` 与奶白 `#fffdf8` 对比约 7.5:1，满足正文/大号文字可读性。

### 2. 顶栏 TopBar
- 背景半透明奶白 `rgba(246,241,230,.92)` + 毛玻璃模糊。
- **下缘 2px 金黄描边**（`border-bottom:2px solid var(--gold)`）——寺院金顶意象。
- 品牌字 `--ink`，站点小标 `--ink-faint`。

### 3. 侧边导航栏 Sidebar
- 背景 `--bg`；搜索框白底、聚焦时金褐描边。
- 目录层级"越大越深"：一级最深深红 `#6e1614` → 二级深红 `#8a1f1c` → 三级金黄 `#b8893b` → 四级浅褐 `#9b8475`。
- **当前激活项**：深红底 + 白字 + 左侧 3px 金黄内阴影（`box-shadow:inset 3px 0 0 var(--gold)`），如佛龛中供灯。

### 4. 按钮 Buttons
- 主按钮（播放 ▶、跳转）：深红底白字，悬停浅红 `--accent-soft`。
- 进退按钮（-15 / +15）：深红文字 + 浅描边，悬停浅暖底 + 金边。
- 文字链接：深红，悬停浅红。

### 5. 卡片 / 专辑卡 Cards
- 表面 `--surface` 近白；描边用浅金 `--gold-soft`（如唐卡边框）。
- 专辑卡内的跳转按钮沿用深红描边样式。

### 6. 首页导览 HomeNav
- 板块标题 `--ink`，描述 `--ink-faint`。
- 目录层级色与侧栏一致（深红→金黄递进）。
- 音频项带 🔊，播放中态深红加粗 + 浅暖底 + 金边。

### 7. 面包屑 / 元信息
- 面包屑：当前 `--ink` 加粗，祖先 `--ink-soft` 悬停深红。
- 标签胶囊：浅暖底 + 次级文字。

### 8. 全局播放器 Player（默认占据屏幕下 1/3）
- **面板**：表面 `--surface` + **顶部 3px 金黄饰条** + 暖色投影。
- **第1行 状态栏**：居中，显示「正在播放：<名称>」，名称深红加粗。
- **第2行 控制按钮**：上一首 ⏮ / 后退15秒 **-15** / 播放 ▶ / 下一首 ⏭ / 前进15秒 **+15**（进退带阿拉伯数字）。
- **第3行 进度条**：两端显示分秒数字（`0:00` … `3:24`），随拖动实时变化；**填充为绿松石 `--turq`**，滑块白心 + 金边。
- **第4行 播放列表开关**：金色描边胶囊按钮「📋 播放列表」+ 浅色小字「点击展开播放列表」（展开后变为「点击收起播放列表」）；右侧 ✕ 关闭。
- **播放列表**：默认收起；点击开关展开，面板自动增高至 72vh，列表项播放中态深红加粗 + 绿松石指示点。
- 悬浮「🎧 播放器」按钮：深红底白字。

### 9. 字号调节 / 统计
- 字号胶囊白底深红字；打开次数统计仅作者可见，浅褐小字。

---

## 四、视觉层级（从强到弱）

1. **深红填充**（主按钮、激活态）——最强视觉权重
2. **金黄描边/饰条**（顶栏、播放器顶饰、卡片边、激活内阴影）——庄严镶边
3. **深红文字/图标**（链接、标题强调、状态栏曲名）——指向性
4. **绿松石动态**（进度、播放指示）——流动清净
5. **暖深棕文字**（正文）——安静可读
6. **浅褐 / 金褐**（提示、分割线）——背景衬托

---

## 五、可读性与响应式

- 全站文字最低对比度 ≥ 4.5:1（正文/大号满足 7:1 级）。
- 移动端（≤760px）：侧栏抽屉化，点击空白关闭；播放器默认 34vh、展开 80vh；控制按钮按比例缩小但保留可点区域。
- 字号可在 90%–150% 间调节，经 `localStorage` 记忆，不影响色彩层级。

---

## 六、与前一版（藏红主题）的差异

- 背景由"浅红"改为"奶白/宣纸"，由暖红调转为暖金调，更贴近寺院素墙与唐卡基底。
- 主色仍取深红（护法），但色值由藏红 `#a3332e` 收敛为更沉的深红 `#8a1f1c`。
- 新增 **金黄** 作为辅助描边色、**绿松石** 作为进度强调色、**靛蓝** 作为冷色次级，形成"红—金—绿—蓝"的藏传五色体系。
- 文字由酱红改为暖深棕，提升长文阅读舒适度与对比度。
