---
title: 更新日志
---

「龙的传人」网站历次推送的关键变更，按时间由新到旧排列。

标签：`新增` 新内容/功能 · `优化` 体验/视觉 · `修复` 问题纠正 · `结构` 目录/导航

---

## 2026-09-02 · 体验优化合集 · [新增] [优化] [修复]

- `新增` 文章目录（TOC）：长文章顶部显示可折叠目录，点击跳转对应章节。
- `新增` 音频播放记忆：记住上次播放的音频和进度，下次打开可继续播放。
- `新增` 返回顶部按钮：页面滚动时出现，点击快速回到顶部。
- `新增` 404 页面：访问不存在的链接时显示友好提示。
- `优化` 法音详情页样式（参照多智钦寺）：标题→原生播放器→诵者→海报。
- `优化` 海报图片 WebP 压缩（加载速度提升 92%）。
- `优化` 音频资料折叠样式（飞书风格三角+提示）。
- `优化` 首页文字大小统一，音频按文章目录顺序排列。
- `新增` 每页访问次数统计（本设备计数）。
- `修复` 法音详情页点击跳转问题。

## 2026-09-02 · 法音详情页 + 音频折叠 + 访问统计 · [新增] [优化] [结构]

- `新增` 上师法音独立详情页（百字明试点），参照多智钦寺样式：标题 → 原生播放器 → 诵者 → 海报，支持在线播放与下载。
- `优化` 海报图片 WebP 压缩（原图 4.98MB → 383KB，加载速度提升 92%）。
- `结构` 上师开示（AI朗读）音频按文章目录顺序排列，与文字版保持一致。
- `优化` 首页文字大小统一：同级标题、同级正文字号一致。
- `新增` 音频资料折叠样式（飞书风格）：分组标题带三角，点击展开/收起，附提示文字。
- `新增` 每个页面访问次数统计（本设备计数），顶部导航栏显示。

## 2026-08-31 · AI 问答 + 划线分享 · [新增] [优化]

- `新增` AI 问答入口（首页卡片 / 导航栏 / 文章页浮动按钮），链接至 ima 知识库。
- `新增` 划线与分享系统：选中文字可划线 / 复制 / 分享，分享生成卡片图片（含文章链接二维码），每篇文章有独立可分享 URL。
- `优化` 点击已划线文字弹出操作菜单（取消划线 / 复制 / 分享）；阅读进度自动记忆与恢复。
- `新增` 2 篇上师开示 AI 朗读音频：修行程序拒绝 DIY、成就的必由之路。

## 2026-08-27 · 导航简化 + SEO + 锁屏播放 · [优化] [新增] [修复] [结构]

- `结构` 导航简化为 5 个一级菜单，点击直接进入各分类 Index 页（完整目录树）。
- `新增` 手机端面包屑目录弹层：点击上一级目录弹出子目录 + 文章列表。
- `优化` SEO 差异化策略：禁止国内搜索引擎（百度 / 搜狗 / 360 / 字节）收录，允许国外（Google / Bing / DuckDuckGo）。
- `修复` 手机锁屏播放控制：添加封面图、audio 元素移入 DOM、提升 iOS 兼容性。
- `优化` 构建时排除「不推送」目录，不便公开的资料仅本地保留。

## 2026-08-26 · 全面优化升级 · [优化] [新增] [修复] [结构]

- `新增` 搜索中文分词、暗色模式切换、Service Worker 离线缓存。
- `优化` 页脚、favicon、栏目首页、书籍页渲染修复。
- `结构` 清理 Quartz 历史残留，移除早课 49 分钟大文件。

## 2026-08-25 · 音频卡可视化 + 首页引导 + 文字页角标 · [优化] [新增]

- `优化` 上师开示聚合页每条音频升级为「浅卡条」：标题独占一行加大、按钮紧随、淡化默认项符（彻底告别"只看到按钮"）。
- `新增` 首页正文加「从哪里开始」引导段：一句主语 + 两个 CTA（📖 从诸行无常开始 / 🎧 直接听上师开示）。
- `新增` 12 篇上师开示文字页自动追加「🎧 音频制作中」金色边角标；后续给页面加入 `[[xxx.mp3]]` 引用后角标自动消失（零维护）。
- `新增` 上师开示「选好你的救命稻草」配音频上线（栏目：2. 找到智慧的指引）。
- `结构` 「关于本站」栏目整理：原 `关于本站.md` 归一为 `index.md` 作为入口页。
- `结构` 音频资源导航补全层级：上师开示（AI朗读）/ 上师法音（2.1 仪轨与经文、2.2 圣号与明咒）/ 上师赞歌 全部进入侧栏，文件夹与专辑一一对应。
- `优化` 内容导航改为手风琴折叠：默认只展开一级标题，点一级展开子级；多个一级可同时展开；展开后按文件夹清晰列出各自音频，可在侧栏直接播放。
- `新增` 为只有音频、没有页面文件的文件夹（如 2. 上师法音及其子文件夹）自动生成索引页（构建期合成、零维护），每个文件夹都有对应可浏览/可播放的页面。
- `优化` 首页音频导览按一级文件夹分组展示（不再平铺 70+ 条）。

## 2026-08-24 · 批量发布上师开示文字 + 播放器升级 · [新增] [优化] [修复]

- `新增` 发布 12 篇「上师开示」文字文章（去除 X 草稿标记，先以纯文字上线，后续逐篇配音频）。
- `新增` 音频播放器「倍速」按钮（0.5x~2x 菜单，实时生效，迷你条同步）。
- `优化` 播放器底部顺序：播放列表（左）→ 调整顺序（中）→ 倍速（右）；上师开示页每篇显示标题。
- `修复` 纠正「云扬」称谓：云扬仅为 TTS 音色、非上师，移除误写的「云 扬上师」。

## 2026-08-18 · 音频列表与「本次更新」板块 · [优化] [结构]

- `优化` 上师开示音频按文章目录排序；子页正文自带播放列表时不再重复附加。
- `新增` 上师开示 🔊 文章归位（按 Obsidian 目录）；新增 2 个音频（羡慕嫉妒恨、求学的关键问题）。
- `修复` AUDIO_TRACKS 重复轨道（标题误用页面标题），重建后 14 条无重复。
- `优化` 「本次更新」板块定型为纯公告栏：仅展示当次新增、不累积历史。

## 2026-08-15 · 导航与播放器体验 · [优化] [新增]

- 隐藏 Obsidian 文件名尾缀 X；导航选中态配色修复；播放器控制顺序与布局优化。
- 新增「本次更新内容」板块、音频子页 index、「龙钦宁提传承」入口。
- 新增播放模式（顺序/逆序/随机/单曲）；process_content 批处理（仅处理 🔊 文章）。

## 2026-08-14 · 网站建设与上线 · [新增] [优化] [结构] [安全]

- 新增「更新日志」页；站点更名「龙的传人｜Longchen Nyingtik」；藏传象征色系重构。
- 正式上线 GitHub Pages（替代 Quartz）；首页导航、全局播放器、藏红主题。

## 2026-08-13 · 切换自研静态站 · [优化]

- 弃用 Quartz，改用零依赖 build_site.py 由 content/ 生成单文件站点。

## 2026-08-10 ~ 08-12 · Quartz 阶段打磨 · [优化]

- 音频内联播放、目录树、字号调节；播放器适老化；清理 56.75MB 大文件。

## 2026-08-09 · 立项与资料初始化 · [新增]

- 确立 Obsidian→静态站→GitHub Pages 架构；整理多智钦寺 202 篇；汇编传承电子书。
