# 龙的传人 · 藏红主题配色规范

> 设计目标：以「藏红（朱红/绛红）」为主色族、「浅红」为背景族，构建一套温暖、庄重、可读性良好的网页/应用界面配色。
> 适用于当前静态站（build_site.py 单文件站点），也可直接迁移到其它前端项目。

---

## 一、配色总览

```
主色族（藏红）      #A3332E  主色 / 链接 / 按钮
                   #C2554E  浅色 / 悬停 / 次级强调
                   #7E221E  深色 / 按下 / 当前激活

背景族（浅红）      #FBF1EF  页面主背景
                   #FC EDEA  区块交替底色
                   #FFF8F6  卡片 / 播放条表面
                   #FAE9E6  引用块 / 代码 / 标签浅红底
                   #F6E0DC  悬停态浅红底

文字族             #2E1A18  主文字 / 标题
                   #876765  次级文字
                   #B49590  三级文字 / 提示

线条族             #F0D7D2  普通分割线 / 边框
                   #E6C2BC  强调分割线
```

---

## 二、颜色令牌明细（HEX / RGB / 使用场景）

### 1. 背景族（浅红系）

| 令牌 | HEX | RGB | 使用场景 |
|---|---|---|---|
| `--bg` | `#FBF1EF` | 251,241,239 | 页面整体背景（极浅暖红），替代原米白 |
| `--bg-2` | `#FC EDEA` | 252,237,234 | 区块 / 段落交替底色（如首页分区、表格隔行） |
| `--surface` | `#FFF8F6` | 255,248,246 | 卡片、全局播放条、播放列表弹层表面（近白暖红） |
| `--surface-soft` | `#FAE9E6` | 250,233,230 | 引用块、代码块、标签、当前播放项浅红底 |
| `--surface-hover` | `#F6E0DC` | 246,224,220 | 导航项 / 链接 / 列表项的悬停态底色 |

### 2. 文字层级

| 令牌 | HEX | RGB | 使用场景 |
|---|---|---|---|
| `--ink` | `#2E1A18` | 46,26,24 | 主文字、标题（h1–h6）、品牌名、加粗正文；深褐红，保证高对比 |
| `--ink-soft` | `#876765` | 135,103,101 | 次级文字：导航项、列表项、正文中辅助说明、引用文字 |
| `--ink-faint` | `#B49590` | 180,149,144 | 三级文字：面包屑、元信息（作者/来源/标签）、字数/时间提示 |

### 3. 线条（边框 / 分割线）

| 令牌 | HEX | RGB | 使用场景 |
|---|---|---|---|
| `--line` | `#F0D7D2` | 240,215,210 | 普通分割线、卡片边框、输入框边框、侧栏分隔 |
| `--line-strong` | `#E6C2BC` | 230,194,188 | 强调分割线（如标题下划线、关键分区边界） |

### 4. 藏红主色族（核心强调）

| 令牌 | HEX | RGB | 使用场景 |
|---|---|---|---|
| `--accent` | `#A3332E` | 163,51,46 | **主色**：正文链接、按钮描边/填充、目录名、进度条、当前激活导航、播放按钮 |
| `--accent-soft` | `#C2554E` | 194,85,78 | **浅色**：链接悬停、次级强调（目录名、播放点、引用左边框）、播放按钮悬停 |
| `--accent-deep` | `#7E221E` | 126,34,30 | **深色**：按钮按下态、需要更强的当前激活强调（可与 `--accent` 组合） |

---

## 三、典型元素配色映射（速查）

| 界面元素 | 颜色令牌 | 说明 |
|---|---|---|
| 页面背景 | `--bg` | 全站底层 |
| 卡片 / 播放条 / 弹层 | `--surface` | 浮层表面 |
| 引用块 / 代码 / 标签底色 | `--surface-soft` | 浅红底突出内容块 |
| 悬停态（导航、链接、列表） | `--surface-hover` | 轻微加深反馈 |
| 一级标题 (h1/h2) | `--ink` + `--line` 下划线 | 庄重深褐红 |
| 正文 | `--ink` | 高对比 |
| 侧栏导航项（未选） | `--ink-soft` | 次级 |
| 当前选中导航项 | `--accent` 底 + 白字 | 藏红填充，强聚焦 |
| 正文链接 | `--accent` 字 + 悬停 `--accent-soft` | 可点击明确 |
| 按钮（播放/跳转） | `--accent` 描边 + 悬停填充 `--accent` 底白字 | 主操作 |
| 面包屑链接 | `--ink-soft` 字 + 悬停 `--accent` | 层级引导 |
| 面包屑当前页 | `--ink` + 加粗 | 定位 |
| 分割线 / 边框 | `--line` | 柔和分隔 |
| 进度条填充 | `--accent` | 播放进度 |
| 播放指示点 | `--accent-soft` / 播放中 `--accent` | 状态 |
| 元信息 / 提示文字 | `--ink-faint` | 弱化但不消失 |

---

## 四、对比度与可读性（WCAG 参考）

以页面主背景 `--bg (#FBF1EF, L≈0.89)` 与纯白 `--surface (#FFF8F6, L≈0.95)` 为基准：

| 前景 / 背景 | 对比度 | 结论 |
|---|---|---|
| `--ink #2E1A18` / `--bg` | ≈ 13.5 : 1 | 远超 AAA（≥7），标题正文皆优 |
| `--ink-soft #876765` / `--bg` | ≈ 4.4 : 1 | 达 AA（≥4.5）临界，用于次级文字/导航足够 |
| `--ink-faint #B49590` / `--bg` | ≈ 2.6 : 1 | 仅用于装饰性提示（面包屑、元信息），不可作正文 |
| `--accent #A3332E` / 白 `--surface` | ≈ 6.9 : 1 | 达 AA/AAA，链接按钮清晰 |
| `--accent #A3332E` / `--bg` | ≈ 6.2 : 1 | 达 AA，主色在浅红底上醒目 |
| `--accent-soft #C2554E` / 白 | ≈ 4.4 : 1 | 用于悬停/大号强调，正文链接仍用 `--accent` |

**设计纪律**
- 正文、标题、按钮文字一律用 `--ink` 或白字，绝不用 `--ink-faint`。
- 链接默认 `--accent`（保证对比），`--accent-soft` 仅作悬停与大号次级强调。
- 浅红底（`--surface-soft/-hover`）只作底色，其上文字仍用 `--ink` / `--ink-soft`。

---

## 五、与项目的关系

- 本规范已落地于 `build_site.py` 的 `:root` 与全部相关样式，构建后体现在 `dist/index.html`。
- 旧配色（金棕 `--accent:#a07a3c`、米白 `--bg:#fcfbf9`）已整体替换为藏红/浅红族。
- 若后续扩展深色模式，可在此基础上派生：背景转 `#2E1A18`，文字转 `#FBF1EF`，主色保持 `--accent` 或提亮至 `#C2554E`。
