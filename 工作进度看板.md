# 工作进度看板

> 创建于 2026-08-19（周三）。本看板为**本地进度追踪文档**，**不纳入网站 git 仓库**（保持 untracked，不随网页发布）。
> 更新频率：每天一次。由 AI 协助盘点，将当日进展追加至文末「每日记录」。

## 一、当前进度主线

### [进行中] 龙的传人网站（GitHub Pages · 藏红/浅红主题）
线上地址：https://drimednor.github.io/longchen-nyingtik/

**近期已完成（2026-08-18 ~ 08-19 收尾）**
- 首页「最近更新」公告区最终定稿并上线：
  - 大标题「最近更新 · 当天日期」，日期由 `content/本次更新内容.md` 的 frontmatter `date` 字段动态注入（变量 `HOME_UPDATE_DATE`），非硬编码。
  - 内容只列当次新增（当前 8/18 两个音频：羡慕嫉妒恨的结果、求学的关键问题）；纯公告栏、不累积旧资料；无新增时整段不显示。
  - 最后提交 `811a2b9` 已推送 v5，本地 = 远程一致。
- 音频系统修复：`AUDIO_TRACKS` 去重 + 标题取文件名（线上 14 条、0 重复）；mp3 弯引号 404 修复（改名 + 同步 wikilink）。
- 更新日志 2026-08-15 段落标签统一（补 10 条 `新增/优化` 标签、修「倒 序」笔误）。

**本地未提交改动（截至 2026-08-27 已全部清理，工作树干净）**
- 此前长期累积的上师开示 `X.md` 草稿（约 15 篇）已随 8/24–8/25 批量发布转正，**磁盘上 0 个 `X.md` 残留 → 草稿阶段结束**。
- 非内容残留已清理/忽略：`__pycache__/` 已加入 `.gitignore`（8/27 `f063741`）、`Cloudflare Pages 迁移操作手册.md` 与 `R2音频迁移指南.md` 等本地文档不入库；`3 张 Pasted image 20260818*.png` 不再出现在 status。
- 🆕 新增「不推送」机制（8/27 `b48d25b`）：用户不想公开的音频/资料放入 `content/音频资源/4. 其他（不推送）`，构建期 `build_site.py` 自动排除、且未入库 → 仅本地保留，不上线（关联下一步 #1 的 R2 音频迁移调研）。

### [常态] 其他工作线
- 公众号「小谦的千万行」/「在修行中生活」：常态发文（周一三五），公益纯分享。
- 龙钦宁提传承学习资料库：整合多智钦寺文章/开示/《做一个真正的修行人》，进行中。
- 梵心集：个人收藏库（Obsidian + 飞书云盘 + NAS 双备份）。
- 网约车记账：滴答→飞书双轨，滚动月统计（净日目标 ¥165/¥211）。
- 修行功课：每日 2 小时专注念诵（空行心咒/莲师心咒等）。

## 二、下一步计划（按优先级）
1. [迁移] **网站**：迁至 Cloudflare Pages（+ 可选自定义域名，.com ≈ ¥76/年）。官方文档已调研、操作手册已编制（`Cloudflare Pages 迁移操作手册.md`，本地文档不入库）；🆕 8/26 浮现 `R2音频迁移指南.md`（音频拟迁 Cloudflare R2 存储，本地文档不入库），迁移调研从 Pages 扩展至音频托管。**待你**：注册 Cloudflare 账号 → 授权 GitHub → 填构建配置（分支 `v5` / 命令 `python3 build_site.py` / 目录 `dist`）；你完成后我做线上验证 + `_headers` 缓存优化 + 旧站收尾。（8/27 已先落地「不推送」构建排除机制，私有音频/资料本地化先行，与 R2 音频迁移方向一致）
2. [已完成] **网站**：本地「书籍」目录改动（`书籍/index.md` 改 + `书籍/书单.md` 删）已于 2026-08-23 随提交 `cf1da35` 上线（书籍页改版 + 书单下线），待决策项解除。
3. [进行中] **网站**：上师开示新文章定稿与发布——8/24 发布 12 篇文字（去 X 草稿、迈进「批量发布」）；8/25「选好你的救命稻草」已配音频（2. 找到智慧的指引）；8/26 音频页大优化（浅卡条/首页引导/文字页角标/手风琴导航/自动索引页）；8/27 平台升级（导航简化为一级菜单 + 手机端面包屑弹层 + 锁屏播放两轮修复 + SEO 禁止收录）。**X.md 草稿阶段已结束**（8/27 工作树干净、磁盘 0 残留）。后续：逐篇配音频 + 更新日志/首页公告待补 8/26、8/27 段（当前 `本次更新内容.md` 仍停在 2026-08-25）。
4. [已完成] **运营**：每次 push 后主动提醒硬刷新/无痕查看（GitHub Pages CDN + 手机缓存，已固化为发布约定，8/18 落地；Cloudflare 迁移后此问题预期大幅改善）。（8/23 推送 5 个提交；8/27 又一次大批量推送 7 个提交至 origin/v5，尤其导航结构大改，记得提醒访客/自己硬刷新查看新导航与锁屏播放控制）
5. [进行中] **资料库**：龙钦宁提传承学习资料库按节奏继续整合。
6. [常态] **公众号**：按周一三五节奏发稿；月度复盘。

## 三、每日记录
### 2026-08-19（周三）
- 盘点收尾：确认网站公告区定稿上线（`811a2b9`，本地 = 远程一致）；列出本地未提交改动与下一步。
- 创建本看板，建立每日更新机制（每日 23:10 由 AI 协助盘点并追加记录）。

### 2026-08-19（周三）· 23:10 定时盘点
- **无新增进展**：今日（8/19）无新 git 提交，最近提交仍为 `811a2b9`（8/18 公告区定稿）；`content/本次更新内容.md` 日期仍为 2026-08-18，首页公告区无 8/19 新内容。
- **本地未提交改动仍在累积**（git status 确认）：
  - 「关于本站」index.md 修改、隐私.md 删除 —— 待决策是否上线（见下一步 #1）。
  - 上师开示多篇 `X.md` 草稿持续累积（1.2 诸行无常下 2 篇、4.1 正确的依师之道下 4 篇、4.x 多篇、2. 找到智慧的指引 / 3. 心态决定命运 / 4.2 做一个靠谱的人 等），均未定稿（下一步 #2）。
  - 额外：`content/上师开示/1. 为何修行/index.md` 有修改；3 张 `Pasted image 20260818*.png` 与 `__pycache__`（构建缓存，非内容）。
- **仍在进行**：上师开示新文章骨架撰写（下一步 #2）；龙钦宁提资料库整合（下一步 #4）；公众号按周一三五节奏发文（下一步 #5）。

### 2026-08-20（周四）· 23:10 定时盘点
- **无新增进展**：今日（8/20）及昨日（8/19）均无新 git 提交，最近提交仍为 `811a2b9`（8/18 公告区定稿）；`content/本次更新内容.md` 日期仍为 2026-08-18，首页公告区无 8/19–8/20 新增内容。
- **本地未提交改动仍在累积**（git status 确认，分支 v5）：
  - ⚠️ **更正（针对 8/19 记录）**：待决策的本地改动实际位于 `content/书籍/` 目录——`书籍/index.md` 修改 + `书籍/书单.md` 删除；「关于本站」目录当前**无任何待提交改动**（此前记录误写为关于本站，已同步修正「下一步计划 #1」与「当前进度主线」）。
  - 新增浮现：`content/音频资源/1. 上师开示/index.md` 有修改（音频索引页，8/19 记录未列入）。
  - 上师开示多篇 `X.md` 草稿持续累积（共 9 个：1.2 轮回过患下 2 篇、4.1 正确的依师之道下 3 篇、4.如何修行下 4 篇；新骨架文件夹 `2. 找到智慧的指引/` `3. 心态决定命运/` `4.2 做一个靠谱的人/` 已建但未定稿），均未配置音频（下一步 #2）。
  - 注意：8/19 记录提到的 `上师开示/1. 为何修行/index.md` 修改已不在当前 git status（工作树与 HEAD 一致，疑似已还原/解决）。
  - 非内容：3 张 `Pasted image 20260818*.png` 与 `__pycache__/`（构建缓存）。
- **仍在进行**：上师开示新文章骨架撰写与定稿（下一步 #2）；龙钦宁提资料库整合（下一步 #4）；公众号按周一三五节奏发文（下一步 #5）。
- **提醒**：`工作进度看板.md` 出现在 untracked 列表——按约定不纳入 git，本次未提交、请勿提交。

### 2026-08-21（周五）· 白天手动盘点
- **Cloudflare Pages 迁移启动（用户拍板方案 ②）**：官方文档已逐条调研（Git 集成 / 构建配置 / 自定义域名 / 免费计划限制 / 域名注册），编制 `Cloudflare Pages 迁移操作手册.md`（本地文档，不入库）。要点：免费计划 ¥0（500 构建/月、无带宽费、单文件 ≤25MiB、≤20,000 文件、1 并发构建）；自定义域名可绑（每项目 100 个），.com ≈ $10.44/年（成本价、注册=续费、免费 WHOIS 隐私、支持支付宝）；构建镜像 v3 默认 Python 3.13.3，`build_site.py` 零依赖跨平台已核实，构建命令 `python3 build_site.py`、输出目录 `dist`、生产分支 `v5`；mp3 14 个最大 8.6MiB 无超限。
- **收益**：Cloudflare 对 HTML 不强制缓存 → 有望根治「手机端显示旧版」；无带宽费优于 GitHub Pages 100GB/月软上限。
- **待用户**：注册 Cloudflare 账号（验证邮箱）→ 授权 GitHub → 填三项构建配置并部署；完成后我做线上验证 + `_headers` 缓存优化 + 旧站收尾。
- 无新 git 提交，最近提交仍为 `811a2b9`。

### 2026-08-21（周五）· 23:10 定时盘点
- **无新增进展（代码/内容发布）**：今日（8/21）无新 git 提交，最近提交仍为 `811a2b9`（8/18 公告区定稿）；`content/本次更新内容.md` 日期仍为 2026-08-18，首页公告区无 8/21 新增内容。
- **本地未提交改动仍在累积**（git status 确认，分支 v5）：
  - 待决策（同前）：`content/书籍/index.md` 修改 + `content/书籍/书单.md` 删除（下一步 #2）。
  - 修改浮现：`content/音频资源/1. 上师开示/index.md`（音频索引页）、`content/上师开示/1. 为何修行/index.md` —— 后者在 8/20 记录称「已还原/解决」，**本次 git status 重新出现**，建议下次盘点确认是否再次改动或已还原。
  - 上师开示 `X.md` 草稿**由 9 个增至 15 个**（较 8/20 明显增多）：新增 `2. 找到智慧的指引/` 下 3 篇、`3. 心态决定命运/` 下 2 篇、`4.2 做一个靠谱的人/` 下 1 篇；原有 `1.2 诸行无常` 2 篇、`4.1 正确的依师之道` 3 篇、`4.如何修行` 4 篇仍在。均未配置音频（下一步 #3）。
  - 非内容：`Cloudflare Pages 迁移操作手册.md`（白天手动盘点已记录，本地文档不入库）、3 张 `Pasted image 20260818*.png`、`__pycache__/`（构建缓存）。
- **仍在进行**：上师开示新文章骨架撰写与定稿（下一步 #3）；龙钦宁提资料库整合（下一步 #5）；Cloudflare Pages 迁移待用户注册账号启动（下一步 #1）；公众号按周一三五节奏发文（下一步 #6）。
- **提醒**：`工作进度看板.md` 出现在 untracked 列表——按约定不纳入 git，本次未提交、请勿提交。

### 2026-08-22（周六）· 23:10 定时盘点
- **无新增进展（代码/内容发布）**：今日（8/22）及近几日（8/19–8/22）均无新 git 提交，最近提交仍为 `811a2b9`（8/18 公告区定稿）；`content/本次更新内容.md` 的 `date` 仍为 2026-08-18，首页公告区无 8/19–8/22 新增内容。
- **本地未提交改动仍在累积**（git status 确认，分支 v5）：
  - 待决策（同前）：`content/书籍/index.md` 修改 + `content/书籍/书单.md` 删除（下一步 #2）。
  - 修改浮现：`content/音频资源/1. 上师开示/index.md`（音频索引页，同 8/21；8/20 记录曾漏列）。
  - ⭕ **已澄清（针对 8/21 待确认项）**：`content/上师开示/1. 为何修行/index.md` 修改在 8/21 重新出现，本次 git status 已不再列出（工作树与 HEAD 一致）——已还原/解决，不再累积。
  - 上师开示 `X.md` 草稿持续累积（约 15 个，结构同前）：`1.2 轮回过患` 下 2 篇、`4.1 正确的依师之道` 下 3 篇、`4.如何修行` 下 4 篇（均已列明），以及文件夹 `2. 找到智慧的指引/`、`3. 心态决定命运/`、`4.2 做一个靠谱的人/` 整目录未定稿（本次以目录形式呈现）；均未配置音频（下一步 #3）。
  - 非内容：`Cloudflare Pages 迁移操作手册.md`（本地文档不入库）、3 张 `Pasted image 20260818*.png`、`__pycache__/`（构建缓存）。
- **仍在进行**：上师开示新文章骨架撰写与定稿（下一步 #3）；龙钦宁提资料库整合（下一步 #5）；Cloudflare Pages 迁移待用户注册账号启动（下一步 #1）；公众号按周一三五节奏发文（下一步 #6，8/22 为周六非发文日）。
- **提醒**：`工作进度看板.md` 出现在 untracked 列表——按约定不纳入 git，本次未提交、请勿提交。

### 2026-08-23（周日）· 23:10 定时盘点
- **🎉 重大突破：8/18 以来首次实质更新，且已推送线上。** 今日（8/23）共有 5 个新提交，最新 `046ac48`；经 `git ls-remote` 核对 origin/v5 头部同为 `046ac48` → **本地 = 远程一致，线上已更新**（GitHub Pages 构建后生效）。`content/本次更新内容.md` 的 `date` 已升至 2026-08-23，`更新日志` 新增 2026-08-23 段落。
- **当日新增（代码 + 内容，随提交上线）**：
  - `cf1da35` 内容更新：新增上师开示音频「享乐的尽头，是堕落」（mp3 入库）+ **书籍页改版并重新发布、原书单页下线** + 关于本站页更新 + 诸行无常页修订 + 同步首页公告与更新日志。
  - `4a2e808` 修复：音频资源「上师开示」聚合页补入新音频「享乐的尽头，是堕落」（解决手动列表漏播）。
  - `86cecbb` 优化：首页引言删除「先是为了自己好好学习」一句（用户要求）。
  - `7637a1d` 新增：音频播放器「倍速」按钮（0.5x~2x 实时生效，迷你条同步，切歌保持）。
  - `046ac48` 优化：播放器底部按钮顺序调整为 播放列表(左)→调整顺序(中)→倍速(右)，倍速菜单右对齐。
- **待决策项解除**：原「下一步计划 #2」本地书籍目录改动（`书籍/index.md` 改 + `书籍/书单.md` 删）已于今日随 `cf1da35` 提交并上线；git 确认 `书籍/书单.md` 已从仓库移除、`书籍/index.md` 已跟踪。
- **本地未提交改动仍在累积**（git status 确认，分支 v5；均按约定暂不入库）：
  - 上师开示 `X.md` 进行中草稿（未定稿，下一步 #3）：`1.2 轮回过患/千万不要滞留在中阴间X.md`；`2. 找到智慧的指引/`、`3. 心态决定命运/`、`4.2 做一个靠谱的人/` 整目录；`4.1 正确的依师之道/` 下 3 篇（修行程序拒绝 / 做一个合格的密行者 / 成就的必由之路）；`4.如何修行/` 下 4 篇（为什么要把修行放在第一？/ 修行人的行为规则 / 如何反观与自省 / 新年第一天的发心）。
  - 🆕 浮现：`content/上师开示/1. 为何修行？/1.2 轮回过患/享乐的尽头，是堕落🔊.md`——已发布音频对应的独立文章页，尚未提交，待定稿后走发布流程。
  - 非内容：`Cloudflare Pages 迁移操作手册.md`（本地文档不入库）、`__pycache__/`、3 张 `Pasted image 20260818*.png`（构建缓存/图片素材）。
- **仍在进行**：上师开示新文章骨架撰写与定稿（下一步 #3，今日 1 篇已成型但整批仍在进行）；龙钦宁提资料库整合（下一步 #5）；Cloudflare Pages 迁移待用户注册账号启动（下一步 #1）；公众号按周一三五节奏发文（下一步 #6，8/23 为周日非发文日）。
- **提醒**：`工作进度看板.md` 出现在 untracked 列表——按约定不纳入 git，本次未提交、请勿提交。

### 2026-08-24（周一）· 23:10 定时盘点
- **🎉 当日重大进展：8/24 有 2 个新提交并推送线上，本地 = 远程一致。** 经 `git ls-remote` 核对 origin/v5 头部为 `919c259`，与本地 HEAD 完全相同 → 线上（GitHub Pages 构建后）已是最新。`content/本次更新内容.md` 的 `date` 升至 2026-08-24，首页公告区已更新。
- **当日新增（代码 + 内容，已随提交上线）**：
  - `125bb74`（00:33）修复：纠正严重错误——「云扬」仅为 TTS 音色、非上师，移除音频「享乐的尽头，是堕落」标题中误加的「上师」称谓。
  - `919c259`（10:16）发布 12 篇「上师开示」文字文章（去除 X 草稿标记，先以纯文字上线，后续逐篇配音频）；更新日志精简（合并 8/24 关键段落、保留 `新增/优化/修复` 标签）；首页公告更新。
- **里程碑**：上师开示文字文章从「草稿累积」进入「批量发布」阶段（下一步 #3 取得实质突破，已同步更新 #3 状态）。
- **本地未提交改动仍在累积**（git status 确认，分支 v5；均按约定暂不入库）：
  - `build_site.py` 修改（构建脚本调整，具体用途待确认）。
  - `content/书籍/1. 千万不能滞留中阴间/index.md` 修改；该目录下 mp3 由 `享乐的尽头，是堕落_云扬.mp3`（已删）重命名为 `享乐的尽头，是堕落.mp3`（新增），并新增 `如何正确的依师成就.mp3`、`下载语音对应的文字内容.mp3` 两枚音频（与 125bb74「云扬」称谓纠正逻辑一致）。
  - 非内容：`Cloudflare Pages 迁移操作手册.md`（本地文档不入库）、`__pycache__/`、3 张 `Pasted image 20260818*.png`（构建缓存/图片素材）。
  - 📌 `工作进度看板.md` 本身（本文件）按约定不纳入 git，始终为 untracked。
- **仍在进行**：上师开示 12 篇文字后续逐篇配音频（下一步 #3 余下工作）；其余 X.md 草稿定稿；龙钦宁提资料库整合（下一步 #5）；Cloudflare Pages 迁移待用户注册账号启动（下一步 #1）；公众号按周一三五节奏发文（下一步 #6，8/24 为周一，应有发文）。
- **提醒**：`工作进度看板.md` 出现在 untracked 列表——按约定不纳入 git，本次未提交、请勿提交。

### 2026-08-25（周二）· 补录（8/26 运行回溯）
- **补录说明**：8/25 当晚 23:10 定时运行未留下记录，现据 git 历史回溯补录（非实时快照）。
- **当日 3 个新提交并推送（git log 日期 2026-08-25）**：
  - `8a2f0bc` 优化：音频条目可视化为「浅卡条」（标题独占一行 + 按钮随行）；首页新增「从哪里开始」引导段（主语 + 2 个 CTA）；12 篇上师开示文字页自动追加「🎧 音频制作中」金色角标（零维护，配音频即消失）。
  - `b998e96` 迁移准备：新增 Cloudflare Pages `_headers`（HTML no-cache 根治旧版缓存、音频长缓存、noindex）+ 部署指南；`build_site.py` 构建时自动复制 `_headers` 到 `dist`。
  - `9e962d0` site：上师开示「选好你的“救命稻草”」配音频（栏目 2. 找到智慧的指引）；「关于本站」归一为 `index.md` 入口。
- **公告同步**：`content/本次更新内容.md` 的 `date` 升至 2026-08-25，`更新日志` 新增 2026-08-25 段落（音频卡可视化 + 首页引导 + 文字页角标 + 配音频 + 关于本站归一 + 音频导航手风琴 + 自动索引页等）。
- **仍在进行**（据后续状态回溯）：上师开示 12 篇文字后续逐篇配音频；龙钦宁提资料库整合；Cloudflare Pages 迁移待用户注册账号（R2 音频迁移调研起步）。

### 2026-08-26（周三）· 23:10 定时盘点
- **🎉 当日实质更新：1 个新提交并推送线上，本地 = 远程一致。** `git ls-remote origin v5` 头部为 `1fba694`，与本地 HEAD 完全相同 → 线上（GitHub Pages 构建后）已是最新。
- **当日新增（代码，已随提交 `1fba694` 上线）**：
  - `1fba694`（8/26）全面优化升级：搜索分词、暗色模式、`sw.js`（PWA 离线/缓存）、页脚、`favicon`、栏目首页、锁屏播放、导航修复、书籍页渲染修复、Quartz 残留清理、移除「早课 49 分钟」、放弃微信验证。
- **⚠️ 待补充公告**：`content/本次更新内容.md` 仍 `date: 2026-08-25`、`更新日志` 最新段仍为 2026-08-25 —— 上述 8/26 大优化尚未写入更新日志与首页公告。**建议后续发布时补「8/26 优化」段落**（注：更新日志/本次更新内容为网站内容文件，本盘点仅记录、未修改）。
- **本地未提交改动**（git status -s 确认，分支 v5；均按约定暂不入库）：
  - 删（working tree，未 staged）：`content/音频资源/2. 上师法音/2.1 仪轨与经文/` 下 7 个 mp3（怙主第四世多智钦·土登琛列华桑仁波切速疾转世祈请文、第五世多智钦土登龙洋仁增嘉措长久住世祈祷文 ×2、第四世多智钦·土登琛列华桑仁波切速疾转世祈请文、简供 ×2、荟供道歌）→ 待确认是否重组织/迁移音频。
  - 增（untracked）：`R2音频迁移指南.md`（本地文档不入库，关联下一步 #1，音频拟迁 Cloudflare R2）；`content/音频资源/4. 其他/` 新目录（新音频分类，未定稿）。
- **仍在进行**：上师开示 12 篇文字后续逐篇配音频（#3 余下，8/25 已配 1 篇「选好你的救命稻草」）；其余 X.md 草稿定稿；龙钦宁提资料库整合（#5）；Cloudflare Pages 迁移待用户注册账号（#1，R2 指南浮现说明调研扩展至音频托管）；公众号周一三五发文（#6，8/26 周三应有发文）。
- **⚠️ 看板入库状态更正**：`工作进度看板.md` 并非如先前记录所称「始终 untracked」——经 `git ls-files` 核实，它**已被纳入仓库**（committed in `9e962d0` 于 8/25，推测 8/25 提交时随 `git add` 一并入库）。本运行仅更新其内容、未提交；为兑现「不纳入 git 仓库」约定，建议后续执行 `git rm --cached 工作进度看板.md`（移除索引、保留本地文件）使其恢复 untracked，并避免日后 `git add -A`/提交时再次卷入。

### 2026-08-27（周四）· 23:10 定时盘点
- **🎉 当日重大进展：8/27 共有 7 个新提交并推送线上，本地 = 远程一致。** `git ls-remote origin v5` 头部为 `3e57f7e`，与本地 HEAD 完全相同 → 线上（GitHub Pages 构建后）已是最新。`git status -s` 为空 → **工作树干净**，此前长期累积的未提交改动至此全部清理。
- **当日新增（代码 + 结构，已随提交 `b48d25b`→`3e57f7e` 上线）**：
  - `b48d25b` 修复：构建时排除「不推送」目录——用户不想公开的音频/资料仅本地保留，不上线。
  - `9864664` 修复：手机锁屏不显示播放控制——Media Session 补 artwork 封面图（assets/cover.png）+ iOS playsInline。
  - `b92804a` 修复：锁屏播放控制第二轮修复——audio 元素移入 DOM + preload=metadata + play 事件重设 metadata，提升 iOS 兼容性。
  - `f063741` 优化：禁止搜索引擎收录——HTML noindex meta + robots.txt(Disallow all) + `_headers` 全局 X-Robots-Tag；清理 `__pycache__` 并加入 `.gitignore`。
  - `b4d56bc` seo：差异化爬虫策略——禁止国内搜索引擎（百度/搜狗/360/字节）收录、允许国外（Google/Bing/DuckDuckGo）收录。
  - `1c3e42f` 优化：手机端面包屑目录弹层——点上一级目录弹出子目录+文章列表，桌面端保持跳转。
  - `3e57f7e` 优化：导航简化为一级菜单——点击直接进入各分类 Index 页（完整目录树）；更新日志并入关于本站；去除侧边栏多级折叠。
- **长期待办清零**：此前累积约 15 篇上师开示 `X.md` 草稿，已随 8/24–8/25 批量发布转正，**磁盘 0 个 `X.md` 残留、工作树干净** → 草稿阶段正式结束（详见「当前进度主线」该 bullet 已同步更新）。
- **⚠️ 公告缺口扩大**：`content/本次更新内容.md` 仍 `date: 2026-08-25`、`content/关于本站/更新日志.md` 最新段仍为 2026-08-25 —— 8/26 全面优化（`1fba694`）+ 8/27 的 7 项提交均**未写入**更新日志与首页公告。**建议后续发布时补「8/26 优化」「8/27 导航/SEO/锁屏播放升级」两段**（注：更新日志/本次更新内容为网站内容文件，本盘点仅记录、未修改）。
- **仍在进行**：上师开示 12 篇文字后续逐篇配音频（#3 余下工作，8/25 仅配 1 篇）；龙钦宁提资料库整合（#5）；Cloudflare Pages + R2 音频迁移待用户注册账号（#1，8/27 已先落地「不推送」本地化机制）；公众号周一三五节奏发文（#6，8/27 周四非发文日）。
- **提醒**：`工作进度看板.md` 当前为 tracked（8/25 `9e962d0` 入库），本运行仅更新内容、未提交（按约定不纳入 git 提交）；如要恢复 untracked 见 8/26 记录建议的 `git rm --cached`。

### 2026-08-28（周五）· 23:10 定时盘点
- **无新增进展（代码/内容发布）**：今日（8/28）无新 git 提交，最近提交仍为 `3e57f7e`（8/27 导航/SEO/锁屏播放升级）；`git ls-remote` 核对 origin/v5 头部同为 `3e57f7e` → **本地 = 远程一致，线上保持 8/27 状态**。`content/本次更新内容.md` 的 `date` 仍为 2026-08-25，首页公告区无 8/26–8/28 新内容。
- **本地未提交改动（git status 确认，分支 v5；均按约定暂不入库）**：
  - 仅 2 项：① `工作进度看板.md` 本身（被 8/25 `9e962d0` 纳入仓库，tracked，本次仅更新内容、未提交）；② 🆕 `content/2026-08-28.md` 为空文件（0 字节，疑似占位草稿，无实质内容、不计入新增发布）。
  - 工作树其余部分干净（8/27 已清零）。
- **⚠️ 公告缺口持续扩大**：`content/本次更新内容.md` 与 `content/关于本站/更新日志.md` 最新段仍停在 2026-08-25，8/26 全面优化（`1fba694`）+ 8/27 七提交 + 8/28 当日**均未写入** → 缺口已累计 3 天。**建议后续发布时补「8/26 优化」「8/27 导航/SEO/锁屏播放升级」两段**（注：更新日志/本次更新内容为网站内容文件，本盘点仅记录、未修改）。
- **仍在进行**：上师开示 12 篇文字后续逐篇配音频（#3 余下，8/25 仅配 1 篇「选好你的救命稻草」）；龙钦宁提资料库整合（#5）；Cloudflare Pages + R2 音频迁移待用户注册账号（#1，8/27 已先落地「不推送」本地化机制）；公众号周一三五节奏发文（#6，8/28 周五应有发文）。
- **提醒**：`工作进度看板.md` 当前为 tracked（8/25 `9e962d0` 入库），本运行仅更新内容、未提交（按约定不纳入 git 提交）。

### 2026-08-29（周六）· 23:10 定时盘点
- **无新增进展（代码/内容发布）**：今日（8/29）无新 git 提交，最近提交仍为 `3e57f7e`（8/27 导航/SEO/锁屏播放升级）；`git ls-remote` 核对 origin/v5 头部同为 `3e57f7e` → **本地 = 远程一致，线上保持 8/27 状态**。`content/本次更新内容.md` 的 `date` 仍为 2026-08-25，首页公告区无 8/26–8/29 新内容。
- **本地未提交改动（git status 确认，分支 v5；均按约定暂不入库）**：
  - 仅 2 项，与 8/28 完全一致：① `工作进度看板.md` 本身（被 8/25 `9e962d0` 纳入仓库，tracked，本次仅更新内容、未提交）；② `content/2026-08-28.md`（0 字节空文件，疑似占位草稿，无实质内容、不计入新增发布，昨日已浮现、今日仍在）。
  - 工作树其余部分干净（8/27 已清零）。
- **⚠️ 公告缺口持续扩大**：`content/本次更新内容.md` 与 `content/关于本站/更新日志.md` 最新段仍停在 2026-08-25，8/26 全面优化（`1fba694`）+ 8/27 七提交 + 8/28 + 8/29 当日**均未写入** → 缺口已累计 4 天。**建议后续发布时补「8/26 优化」「8/27 导航/SEO/锁屏播放升级」两段**（注：更新日志/本次更新内容为网站内容文件，本盘点仅记录、未修改）。
- **仍在进行**：上师开示 12 篇文字后续逐篇配音频（#3 余下，8/25 仅配 1 篇「选好你的救命稻草」）；龙钦宁提资料库整合（#5）；Cloudflare Pages + R2 音频迁移待用户注册账号（#1，8/27 已先落地「不推送」本地化机制）；公众号周一三五节奏发文（#6，8/29 周六非发文日）。
- **下一步计划状态**：较 8/28 无变化——#2 书籍上线、#4 运营 push 提醒均为 [已完成]；#1 迁移、#3 上师开示发布、#5 资料库为 [进行中]；#6 公众号为 [常态]。无新完成项需勾选。
- **提醒**：`工作进度看板.md` 当前为 tracked（8/25 `9e962d0` 入库），本运行仅更新内容、未提交（按约定不纳入 git 提交）。
