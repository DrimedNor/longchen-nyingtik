# Cloudflare R2 音频迁移指南

> 目标：将网站音频文件从 GitHub 仓库迁移到 Cloudflare R2 对象存储，实现大文件稳定托管、无出口流量费、国内可访问。
> 预计成本：0 元/月（10GB 存储 + 无限流量免费额度内）

---

## 目录

1. [前期确认](#1-前期确认)
2. [第一部分：开通 Cloudflare R2](#2-第一部分开通-cloudflare-r2)
3. [第二部分：配置自定义域名](#3-第二部分配置自定义域名)
4. [第三部分：批量上传音频](#4-第三部分批量上传音频)
5. [第四部分：修改网站代码](#5-第四部分修改网站代码)
6. [第五部分：清理 GitHub 仓库](#6-第五部分清理-github-仓库)
7. [验证与回滚方案](#7-验证与回滚方案)

---

## 1. 前期确认

### 1.1 域名确认

你提到用 `Longchen-Nyingtik` 作为自定义域名。请确认：

- [ ] **情况 A**：你已经拥有 `longchen-nyingtik.com`（或 .org / .net 等）域名
- [ ] **情况 B**：你还没有域名，需要购买
- [ ] **情况 C**：不想买域名，先用 R2 自带免费域名 `xxx.r2.dev`

> 如果是情况 B，域名费用约 ¥50-100/年（.com 约 ¥70/年），在 Cloudflare 注册最便宜（Cloudflare 按成本价卖域名，不加价）。
> 如果是情况 C，可以跳过第二部分，直接用 R2 默认域名。

### 1.2 当前音频规模

```
音频文件总数：约 73 个
音频总大小：约 500MB - 1GB
最大单个文件：约 28MB（护法.mp3）
```

远低于 R2 免费额度（10GB 存储），迁移后月费用 = 0 元。

---

## 2. 第一部分：开通 Cloudflare R2

### 2.1 注册 Cloudflare 账号

1. 访问 https://dash.cloudflare.com/sign-up
2. 用邮箱注册（建议用常用邮箱，方便接收告警）
3. 验证邮箱

### 2.2 开通 R2 服务

1. 登录后，在左侧菜单找到 **R2** → 点击 **Overview**
2. 点击 **Create bucket**（创建存储桶）
3. 存储桶名称：`longchen-nyingtik-audio`（全小写，不能有空格）
4. 位置：选择 **Auto**（自动选择最近节点）
5. 点击 **Create bucket**

### 2.3 绑定信用卡（可能需要）

- Cloudflare 开通 R2 时可能要求绑定信用卡（防止滥用）
- **只要在免费额度内就不会扣费**
- 绑定后建议设置消费告警：
  - 进入 **My Profile** → **Billing** → **Notifications**
  - 添加告警：月消费超过 $0.01 时邮件通知

### 2.4 配置存储桶公开访问

1. 进入刚创建的存储桶 `longchen-nyingtik-audio`
2. 点击 **Settings** 标签
3. 找到 **Public access**（公开访问）
4. 点击 **Allow Access**（允许公开访问）
5. 确认后会得到一个公开访问域名：
   ```
   https://longchen-nyingtik-audio.<你的账号ID>.r2.dev
   ```
   > 这个域名可以直接用，不需要自定义域名。如果不想买域名，记下这个地址，跳到第四部分。

### 2.5 获取 API 凭证（用于上传）

1. 进入 **R2** → **Overview** → 点击 **Manage R2 API Tokens**
2. 点击 **Create API token**
3. 名称：`audio-upload`
4. 权限：选择 **Edit**（读写权限）
5. 存储桶：选择 **Apply to specific buckets only** → 选 `longchen-nyingtik-audio`
6. 点击 **Create API Token**
7. **重要**：复制并保存以下信息（只显示一次）：
   - Access Key ID
   - Secret Access Key
   - Endpoint（如 `https://<账号ID>.r2.cloudflarestorage.com`）

---

## 3. 第二部分：配置自定义域名

> 如果你选择用 R2 默认域名（不买域名），跳过这一部分。

### 3.1 将域名添加到 Cloudflare

**如果你已经有域名（在其他注册商购买）：**

1. 登录 Cloudflare → 点击 **Add a site**
2. 输入你的域名（如 `longchen-nyingtik.com`）
3. 选择免费套餐（Free plan）
4. Cloudflare 会扫描现有 DNS 记录
5. 按照提示，去你的域名注册商处修改 Nameserver 为 Cloudflare 提供的两个地址
6. 等待 DNS 生效（通常几分钟到几小时）

**如果你还没有域名，想在 Cloudflare 购买：**

1. 登录 Cloudflare → 点击 **Register a domain**
2. 搜索 `longchen-nyingtik`
3. 选择后缀（.com 约 $10/年，.org 约 $12/年）
4. 完成购买（Cloudflare 按成本价，不加价）
5. 购买后域名自动在 Cloudflare 管理，无需修改 Nameserver

### 3.2 配置 R2 自定义域名

1. 进入 R2 存储桶 `longchen-nyingtik-audio` → **Settings**
2. 找到 **Custom Domains**（自定义域名）
3. 点击 **Connect Domain**
4. 输入：`audio.longchen-nyingtik.com`（用你的实际域名）
5. 点击 **Continue**
6. Cloudflare 会自动配置 DNS 记录（CNAME）和 SSL 证书
7. 等待几分钟，域名生效后可以通过 `https://audio.longchen-nyingtik.com` 访问

### 3.3 验证自定义域名

在浏览器访问：
```
https://audio.longchen-nyingtik.com
```
如果显示 XML 格式的存储桶列表（或 "Access Denied"），说明域名配置成功。

---

## 4. 第三部分：批量上传音频

### 4.1 安装 rclone（上传工具）

rclone 是命令行工具，支持批量上传到 R2。

**Windows 安装：**

1. 访问 https://rclone.org/downloads/
2. 下载 Windows 版本（Intel/AMD - 64 Bit）
3. 解压到一个目录，如 `C:\rclone\`
4. 将 `C:\rclone\` 添加到系统 PATH 环境变量
5. 打开命令行，输入 `rclone version` 验证安装

### 4.2 配置 rclone

1. 命令行输入：
   ```
   rclone config
   ```
2. 输入 `n` 新建一个远程
3. 名称：`r2audio`
4. 存储类型：选择 **S3**（输入对应的数字）
5. Provider：选择 **Cloudflare**
6. env_auth：选择 `false`
7. access_key_id：粘贴你之前保存的 Access Key ID
8. secret_access_key：粘贴你之前保存的 Secret Access Key
9. region：输入 `auto`
10. endpoint：粘贴你之前保存的 Endpoint（如 `https://<账号ID>.r2.cloudflarestorage.com`）
11. 其他选项按默认回车
12. 输入 `q` 退出配置

### 4.3 批量上传音频

1. 进入项目音频目录：
   ```
   cd D:\Users\Drimed\Projects\龙的传人-website\content\音频资源
   ```

2. 执行上传（同步整个目录到 R2）：
   ```
   rclone sync . r2audio:longchen-nyingtik-audio/音频资源 --progress --transfers 5
   ```
   - `--progress`：显示上传进度
   - `--transfers 5`：同时上传 5 个文件
   - `sync`：让目标和源一致（源没有的文件会在目标删除）

3. 等待上传完成（约 500MB-1GB，根据网速可能需要 5-30 分钟）

### 4.4 验证上传

1. 命令行查看 R2 上的文件：
   ```
   rclone lsd r2audio:longchen-nyingtik-audio/
   ```
2. 或者在浏览器访问：
   ```
   https://audio.longchen-nyingtik.com/音频资源/
   ```
   （用你的实际域名或 r2.dev 域名）

---

## 5. 第四部分：修改网站代码

> 这部分由我（助手）来完成，你只需要确认音频域名。

### 5.1 需要你提供的信息

请告诉我你的音频访问域名，二选一：

- **自定义域名**：`https://audio.longchen-nyingtik.com`
- **R2 默认域名**：`https://longchen-nyingtik-audio.<账号ID>.r2.dev`

### 5.2 我会做的修改

1. 在 `build_site.py` 中添加音频域名配置变量
2. 修改音频路径生成逻辑：从本地相对路径改为 R2 完整 URL
3. 修改播放器代码：音频 src 指向 R2
4. 重新构建网站，验证播放器正常工作
5. 提交并推送代码

### 5.3 修改示例

```python
# build_site.py 顶部添加配置
AUDIO_BASE_URL = "https://audio.longchen-nyingtik.com"  # 你的音频域名

# 音频路径生成时
audio_url = f"{AUDIO_BASE_URL}/音频资源/{category}/{filename}"
```

---

## 6. 第五部分：清理 GitHub 仓库

> 音频迁移到 R2 后，GitHub 仓库里不再需要存大音频文件。这一步可以减小仓库体积，避免 GitHub 限制。

### 6.1 风险说明

- 这一步会从 **git 历史**中删除所有音频文件，需要强制推送
- 强推不可逆，操作前建议备份
- **当前工作区的音频文件不会被删除**（只是从 git 版本控制中移除），本地文件保留
- 网站功能不受影响（因为音频已经从 R2 加载）

### 6.2 执行步骤（由我完成）

1. 将 `content/音频资源/` 加入 `.gitignore`
2. 从 git 暂存区移除音频文件：`git rm --cached -r content/音频资源/`
3. 提交：`git commit -m "chore: 音频迁移到 R2，从 git 移除大文件"`
4. （可选）用 `git filter-repo` 从历史中彻底删除音频，进一步减小仓库体积
5. 强制推送：`git push --force origin v5`

### 6.3 关于 git filter-repo

- 如果只做步骤 1-3，音频文件从最新 commit 中移除，但历史中仍然存在，仓库体积不会立刻变小
- 如果做步骤 4（filter-repo），音频从整个历史中删除，仓库体积大幅减小，但需要强推
- **建议**：先做步骤 1-3（安全，不强推），等以后仓库体积真的逼近限制时再做 filter-repo

---

## 7. 验证与回滚方案

### 7.1 验证清单

- [ ] 网站首页正常加载
- [ ] 导航正常
- [ ] 文章页正常
- [ ] 音频页列表正常
- [ ] 点击音频可以播放（测试 3-5 个不同音频）
- [ ] 播放器进度条、音量、暂停正常
- [ ] 手机端播放正常
- [ ] 锁屏播放控制正常（Media Session API）
- [ ] 页面加载速度（音频不阻塞页面加载）

### 7.2 回滚方案

如果迁移后出现问题，可以快速回滚：

1. **代码回滚**：`git revert` 或 `git reset` 到迁移前的 commit
2. **音频回滚**：如果 R2 不可用，临时把音频域名改回 GitHub 本地路径
3. **本地备份**：迁移前所有音频文件都在本地 `content/音频资源/`，不会丢失

---

## 附录：费用监控

### 设置消费告警

1. Cloudflare → **My Profile** → **Billing** → **Notifications**
2. 添加通知：
   - 类型：Billing
   - 触发条件：月消费超过 $0.01
   - 通知方式：邮件

### 每月检查

每月初登录 Cloudflare → **R2** → **Analytics**，查看：
- 存储使用量（确保 < 10GB）
- 读操作次数（确保 < 1000万/月）
- 出口流量（免费，无限制）

---

## 下一步

1. 你完成第一部分（开通 R2、创建存储桶、获取 API 凭证）
2. 告诉我你的音频访问域名（自定义域名或 r2.dev 域名）
3. 我完成第四部分（修改代码）和第六部分（清理仓库）
4. 一起验证

有任何步骤卡住了，随时告诉我。
